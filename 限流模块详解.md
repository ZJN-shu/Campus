# 限流模块详解

## 目录

1. [模块概述](#一模块概述)
2. [核心组件](#二核心组件)
3. [协作流程](#三协作流程)
4. [技术亮点](#四技术亮点)
5. [面试常见问题](#五面试常见问题)
6. [优化建议](#六优化建议)

---

## 一、模块概述

### 1.1 功能介绍

本模块实现了基于**令牌桶算法**的分布式限流功能，通过**注解** + **拦截器** + **Lua脚本**的组合，对API接口进行请求频率限制，防止恶意刷接口、保障系统稳定性。

### 1.2 技术选型

| 技术 | 选型 | 说明 |
|-----|------|------|
| 限流算法 | 令牌桶算法 | 支持突发流量，用户体验好 |
| 执行位置 | Redis + Lua脚本 | 保证原子性，分布式环境一致 |
| 触发方式 | 注解+拦截器 | 使用简单，侵入性低 |

### 1.3 限流场景配置

| 接口 | 限流策略 | 说明 |
|-----|---------|------|
| 发送验证码 | 5次/小时 | 防止恶意刷短信 |
| 登录 | 10次/分钟 | 防止暴力破解 |
| 发布帖子 | 3次/分钟 | 防止刷屏 |
| 点赞 | 5次/秒 | 防止机器点赞 |
| 评论 | 3次/秒 | 防止刷屏 |

---

## 二、核心组件

### 2.1 限流注解 `@RateLimit`

**文件位置**：[RateLimit.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\annotation\RateLimit.java)

#### 注解作用
- ✅ **声明**：标记需要限流的方法
- ✅ **配置**：设置限流参数
- ✅ **无执行**：只是"贴标签"，不实际执行限流逻辑

#### 注解参数

| 参数 | 类型 | 默认值 | 说明 |
|-----|------|-------|------|
| `capacity` | long | 10 | 令牌桶容量（最大令牌数） |
| `refillRate` | long | 5 | 每秒补充的令牌数 |
| `timeUnit` | TimeUnit | SECONDS | 时间单位 |
| `message` | String | "请求过于频繁..." | 限流提示语 |

#### 使用示例

```java
// 方式1：使用新注解（推荐）
@RateLimit(capacity = 10, refillRate = 5, message = "请求太频繁")
public Result createPost(@RequestBody Post post) { ... }

// 方式2：兼容旧注解
@RateLimit(value = 3, timeout = 1, timeUnit = TimeUnit.MINUTES)
public Result likePost(@PathVariable Long id) { ... }
```

### 2.2 限流拦截器 `RateLimitInterceptor`

**文件位置**：[RateLimitInterceptor.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\interceptor\RateLimitInterceptor.java)

#### 拦截器作用
- ✅ **检测**：请求到达时检查方法是否有`@RateLimit`注解
- ✅ **提取**：从注解中提取配置参数
- ✅ **调用**：执行Lua脚本进行限流检查
- ✅ **裁决**：根据结果决定放行或拦截

#### 核心代码

```java
@Override
public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
    
    // 1. 检查是否有@RateLimit注解
    if (!(handler instanceof HandlerMethod)) return true;
    
    HandlerMethod handlerMethod = (HandlerMethod) handler;
    RateLimit rateLimit = handlerMethod.getMethodAnnotation(RateLimit.class);
    
    if (rateLimit == null) return true;  // 没加注解，直接放行
    
    // 2. 从注解读取参数
    long capacity = rateLimit.capacity();
    long refillRate = rateLimit.refillRate();
    String message = rateLimit.message();
    
    // 3. 生成限流Key（用户级别）
    Long userId = StudentHolder.getStudentId();
    String key = "rate:token:" + (userId != null ? userId : "anonymous") + ":" + requestUri;
    
    // 4. 执行Lua脚本
    List<Long> result = stringRedisTemplate.execute(
        redisScript,
        Arrays.asList(key),
        capacity, refillRate, now, "1"
    );
    
    // 5. 根据结果裁决
    if (result.get(0) == 0) {
        renderErrorResponse(response, message);  // 拦截，返回429
        return false;
    }
    
    return true;  // 放行
}
```

### 2.3 Lua脚本（令牌桶核心）

**文件位置**：[RateLimitInterceptor.java#L35-76](file:///d:\Code\campus%20-%201\src\main\java\com\campus\interceptor\RateLimitInterceptor.java#L35-L76)

#### Lua脚本作用
- ✅ **算法**：实现令牌桶核心逻辑
- ✅ **原子**：所有操作在Redis中一步完成
- ✅ **性能**：Redis服务端执行，减少网络开销

#### Lua脚本代码

```lua
local key = KEYS[1]                    -- Redis Key
local capacity = tonumber(ARGV[1])      -- 令牌桶容量
local refillRate = tonumber(ARGV[2])    -- 补充速率
local now = tonumber(ARGV[3])           -- 当前时间
local requested = tonumber(ARGV[4])     -- 请求的令牌数

-- 1. 获取当前状态
local lastRefillTime = redis.call('HGET', key, 'lastRefillTime')
local tokens = redis.call('HGET', key, 'tokens')

if lastRefillTime == false or tokens == false then
    -- 初始化
    lastRefillTime = now
    tokens = capacity
else
    lastRefillTime = tonumber(lastRefillTime)
    tokens = tonumber(tokens)
end

-- 2. 计算应该补充的令牌数
local timePassed = now - lastRefillTime
local refillTokens = math.floor(timePassed * refillRate)

if refillTokens > 0 then
    tokens = math.min(capacity, tokens + refillTokens)
    lastRefillTime = now
end

-- 3. 尝试获取令牌
local allowed = 0
if tokens >= requested then
    allowed = 1
    tokens = tokens - requested
end

-- 4. 保存状态
redis.call('HMSET', key, 'tokens', tokens, 'lastRefillTime', lastRefillTime)
redis.call('EXPIRE', key, math.ceil(capacity / refillRate) * 2 + 60)

-- 5. 返回结果
return {allowed, tokens, capacity}
```

#### Redis数据结构

使用Hash结构存储令牌桶状态：

```
Key: rate:token:{userId}:{requestUri}
Type: Hash
Fields:
  - tokens: 剩余令牌数
  - lastRefillTime: 上次补充时间（秒）
```

### 2.4 拦截器配置 `MvcConfig`

**文件位置**：[MvcConfig.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\config\MvcConfig.java)

```java
@Configuration
public class MvcConfig implements WebMvcConfigurer {
    
    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        // 注册限流拦截器，优先级最高
        registry.addInterceptor(new RateLimitInterceptor(stringRedisTemplate))
                .addPathPatterns("/**")
                .order(0);  // 先限流，再执行其他拦截器
    }
}
```

---

## 三、协作流程

### 3.1 完整流程图

```
┌─────────────────────────────────────────────────────────────────┐
│  用户发起请求                                                    │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Spring MVC 接收到请求                                           │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  RateLimitInterceptor.preHandle()                              │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ 1. 检查目标方法是否有 @RateLimit 注解？                  │  │
│  │ 2. 有：提取注解参数（capacity=10, refillRate=5）        │  │
│  │ 3. 生成Redis Key: rate:token:{userId}:{uri}            │  │
│  │ 4. 调用Lua脚本，传入参数                               │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  Lua脚本在Redis中执行                                           │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ 1. 获取上次补充时间和当前令牌数                          │  │
│  │ 2. 根据时间差计算应该补充的令牌数                        │  │
│  │ 3. 检查当前令牌是否够用                                  │  │
│  │ 4. 够：扣减令牌，allowed=1                               │  │
│  │ 5. 不够：allowed=0                                       │  │
│  │ 6. 保存新状态到Redis                                     │  │
│  │ 7. 返回结果: [allowed, 剩余令牌, 容量]                   │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                │
                        ┌───────┴───────┐
                        │               │
                        ▼               ▼
┌─────────────────┐  allowed=1  ┌─────────────────┐
│  allowed=0 拦截 │  ────────→  │  allowed=1 放行 │
└─────────────────┘              └─────────────────┘
        │                                │
        ▼                                ▼
┌─────────────────┐              ┌─────────────────┐
│  HTTP 429      │              │  执行业务方法   │
│  {"success":   │              │  Controller    │
│   false, ...}  │              │                │
└─────────────────┘              └─────────────────┘
```

### 3.2 数据流转

```
请求 → @RateLimit注解 → 拦截器检测 → 提取参数 → Lua脚本执行
                                                         ↓
                                                         ↓
返回429 ← 拦截器裁决 ← Lua返回allowed=0 ← 令牌不足
                                                         ↓
                                                         ↓
执行业务 ← 拦截器裁决 ← Lua返回allowed=1 ← 令牌足够
```

---

## 四、技术亮点

### 4.1 为什么选令牌桶算法？

| 算法 | 特点 | 适用场景 | 本项目选择原因 |
|-----|------|---------|--------------|
| **令牌桶** | 支持突发流量，匀速补充 | ✅ API限流 | 允许用户一次性发多个请求（体验好），同时限制平均流量 |
| **计数器** | 严格匀速，无法突发 | ❌ 体验差 | 用户体验不好，容易被"卡点" |
| **滑动窗口** | 平滑但实现复杂 | 一般 | 性能不如令牌桶 |

### 4.2 为什么用Lua脚本？

#### ❌ 错误方式（不用Lua）

```java
// 问题：步骤1和3之间可能被其他请求插入，导致竞态条件
String tokens = stringRedisTemplate.opsForValue().get(key);  // 步骤1：获取令牌数
if (Long.parseLong(tokens) > 0) {                           // 步骤2：判断
    stringRedisTemplate.opsForValue().decrement(key);       // 步骤3：扣减
}
```

**问题场景**：
```
请求A: GET tokens → 10
请求B: GET tokens → 10
请求A: DECREMENT → 9
请求B: DECREMENT → 8
// 两个请求都通过了，但实际上只应该通过1个！
```

#### ✅ 正确方式（用Lua）

```lua
-- 整个逻辑在Redis中一步完成，不会被打断
local tokens = redis.call('HGET', key, 'tokens')
-- ...判断...
redis.call('HSET', key, 'tokens', tokens - requested)
```

### 4.3 用户级别限流

```java
String key = "rate:token:" + userId + ":" + requestUri;
```

- ✅ 防止单个用户过度消耗资源
- ✅ 支持匿名用户（用"anonymous"标识）
- ✅ 不同接口独立限流

### 4.4 过期时间动态计算

```lua
redis.call('EXPIRE', key, math.ceil(capacity / refillRate) * 2 + 60)
```

- 容量/补充速率 = 桶满需要的时间
- 乘以2 + 60秒 = 确保有足够的时间窗口
- 避免长期不访问的Key占用Redis内存

---

## 五、面试常见问题

### 问题1：讲一下你项目中是怎么做限流的？

**面试官考察点**：
- 是否了解限流的整体架构
- 是否知道为什么用这些技术

**回答参考**：

> 我项目中使用的是**令牌桶算法 + Redis + Lua脚本 + 注解 + 拦截器**的方案。
>
> 具体来说：
> 1. **注解**：我在需要限流的Controller方法上添加`@RateLimit`注解，用来声明限流规则（比如10个令牌，每秒补5个）
> 2. **拦截器**：Spring MVC的拦截器在请求到达时，检查方法有没有限流注解，如果有就提取参数
> 3. **Lua脚本**：我用Lua脚本在Redis中原子执行令牌桶逻辑，补充令牌、检查、扣减一步完成，避免竞态条件
>
> 这样实现的好处是：使用简单（加个注解就行）、分布式环境一致、性能好。

---

### 问题2：为什么用令牌桶而不是计数器？

**面试官考察点**：
- 是否了解不同限流算法的区别
- 是否能根据场景选择合适的算法

**回答参考**：

> 我选择令牌桶主要有几个原因：
>
> 1. **用户体验好**：令牌桶允许突发流量，比如用户可以一次性发3个请求（只要令牌够），而计数器是严格匀速的，用户会被"卡点"
> 2. **平滑限流**：令牌桶是匀速补充的，能平滑限制平均流量
> 3. **实现简单**：结合Redis和Lua脚本，实现起来比较简单，性能也很好
>
> 当然，计数器也有它的场景（比如限制每秒总QPS），但在API限流这个场景下，令牌桶更合适。

---

### 问题3：为什么用Lua脚本？不能直接写Java代码吗？

**面试官考察点**：
- 是否理解原子性的重要性
- 是否知道竞态条件的问题

**回答参考**：

> 这里必须用Lua脚本，因为要**保证原子性**。
>
> 如果用Java代码分步写：
> ```java
> String tokens = stringRedisTemplate.get(key);  // 步骤1
> if (tokens > 0) {                            // 步骤2
>     stringRedisTemplate.decrement(key);       // 步骤3
> }
> ```
> 问题是：步骤1和3之间，可能被其他请求插入，导致两个请求都通过了，但实际上令牌不够。
>
> 用Lua脚本的话，整个逻辑在Redis中**一步完成**，不会被打断，所以能保证原子性。

---

### 问题4：你们的限流Key是怎么设计的？

**面试官考察点**：
- 是否考虑了用户级别限流
- 是否考虑了不同接口独立限流

**回答参考**：

> 我们的限流Key是`rate:token:{userId}:{requestUri}`，这样设计有几个考虑：
>
> 1. **用户级别**：每个用户单独一个令牌桶，防止一个用户把所有资源占了
> 2. **接口级别**：不同接口独立限流，比如发帖子和点赞的限流规则可以不一样
> 3. **匿名用户**：没登录的用户用"anonymous"标识，也有一定的保护
>
> 这样既保护了系统，又不会误伤正常用户。

---

### 问题5：如果Redis挂了怎么办？

**面试官考察点**：
- 是否考虑了降级方案
- 是否有容错设计

**回答参考**：

> 这个问题我们考虑过，有几个方案：
>
> 1. **降级放行**：Redis挂了的话，限流功能暂时关闭，让请求直接通过，保证核心业务可用
> 2. **本地限流兜底**：如果Redis不可用，可以用Guava的RateLimiter做本地限流，虽然分布式环境下不准，但至少有点保护
> 3. **监控告警**：监控Redis的健康状态，出问题及时告警
>
> 我们项目中目前实现了第一种方案，在catch异常时直接放行，同时记录日志。

---

### 问题6：如何动态调整限流阈值？

**面试官考察点**：
- 是否考虑了可配置性
- 是否有动态调整的方案

**回答参考**：

> 目前我们是在注解中写死的，但可以优化成动态配置：
>
> 1. **配置中心**：用Nacos/Apollo把限流阈值放在配置中心，动态更新
> 2. **数据库配置**：限流规则存在数据库里，支持后台管理
> 3. **注解支持SpEL**：让注解支持从配置中读取，比如`@RateLimit(capacity="${rate.limit.capacity}")`
>
> 这样可以根据实际流量动态调整限流规则，不需要重新发布代码。

---

### 问题7：如果限流了，怎么给用户提示？

**面试官考察点**：
- 用户体验考虑
- 返回格式设计

**回答参考**：

> 我们限流后返回HTTP 429状态码，加上友好的JSON提示：
>
> ```json
> {
>   "success": false,
>   "errorMsg": "请求过于频繁，请稍后再试"
> }
> ```
>
> 前端收到429后，可以显示友好提示，比如"手速太快啦，休息一下吧~"

---

### 问题8：令牌桶的容量和补充速率怎么定？

**面试官考察点**：
- 是否根据实际场景定参数
- 是否有压测数据支撑

**回答参考**：

> 我们是根据**压测结果**和**业务场景**定的：
>
> 1. **压测**：先压测看系统能扛多少QPS，比如系统极限是1000 QPS，我们留一定余量，比如限到800
> 2. **业务场景**：
>    - 发送验证码：5次/小时（防刷短信）
>    - 登录：10次/分钟（防暴力破解）
>    - 点赞：5次/秒（允许正常操作，防机器刷）
>
> 上线后还会根据实际情况调整。

---

### 问题9：限流模块如何测试？

**面试官考察点**：
- 是否有测试意识
- 知道怎么测并发场景

**回答参考**：

> 我们从几个方面测试：
>
> 1. **单元测试**：测试限流逻辑在正常场景、边界场景下是否正确
> 2. **压力测试**：用JMeter模拟并发请求，看限流是否生效
> 3. **分布式测试**：启动多个实例，看限流是否一致
>
> 压测结果显示：在100并发下，限流准确，没有超卖。

---

## 六、优化建议

### 6.1 短期优化

1. **添加限流监控**：记录限流次数、被限流的用户、接口，方便分析
2. **支持动态配置**：把限流参数放到配置中心
3. **添加降级方案**：Redis不可用时，用本地限流兜底

### 6.2 长期优化

1. **引入限流框架**：考虑用Sentinel或Resilience4j，功能更丰富
2. **多级限流**：
   - 网关层限流（Nginx + Lua）
   - 应用层限流（当前方案）
   - 全局限流（保护整个系统）
3. **热点识别**：自动识别热点接口，动态调整限流阈值

---

## 总结

本限流模块通过**注解**（定义规则） + **拦截器**（检测调度） + **Lua脚本**（核心执行）的组合，实现了一个高性能、分布式、易用的限流功能，能有效保护系统稳定性。

---

**文档版本**：v1.0  
**最后更新**：2026-05-30
