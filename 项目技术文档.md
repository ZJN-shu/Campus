# 校园论坛二手交易系统 - 详细技术报告

## 一、项目概述

这是一个基于Spring Boot 3.x开发的校园论坛二手交易系统，集成了论坛发帖、商品交易、点赞评论、热搜榜、订单管理等功能。项目采用微服务思想设计，使用Redis做缓存、消息队列、限流，Elasticsearch做全文搜索，MySQL做持久化存储。

### 核心特性
- 多级缓存（Caffeine本地缓存 + Redis分布式缓存）
- 异步事件处理（Redis Stream）
- 可靠消息投递（重试机制 + 死信队列）
- 秒杀级库存管理（乐观锁 + Redisson分布式锁）
- 实时热榜（Redis ZSet + 时间衰减算法）
- 健康检查与监控

---

## 二、技术栈选型

| 技术组件 | 版本/选型 | 用途 | 选型理由 |
|---------|---------|------|---------|
| **Spring Boot** | 3.1.5 | 核心框架 | 生态完善，3.x使用Jakarta EE规范 |
| **MyBatis-Plus** | 3.5.9 | ORM框架 | 增强版MyBatis，支持CRUD简化、乐观锁等 |
| **MySQL** | - | 关系型数据库 | 成熟稳定，适合结构化数据 |
| **Redis** | - | 缓存、消息队列、限流 | 高性能、丰富的数据结构 |
| **Redisson** | 3.13.6 | 分布式锁 | 提供完善的分布式锁实现、自动续期 |
| **Caffeine** | 3.1.8 | 本地缓存 | 高性能、基于Java 8，Spring Boot推荐 |
| **Elasticsearch** | 7.12.1 | 全文搜索 | 强大的全文检索能力 |
| **Hutool** | 5.8.27 | 工具库 | 丰富的工具类，简化开发 |
| **Lombok** | - | 简化代码 | 减少样板代码 |

---

## 三、核心模块设计详解

### 3.1 库存管理模块

**文件位置**：[StockCacheService.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\service\Impl\StockCacheService.java)、[OrderServiceImpl.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\service\Impl\OrderServiceImpl.java)

**设计逻辑**：

采用**三阶段库存扣减** + **双重锁机制**：

1. **预扣库存**（创建订单时）
   ```java
   // 乐观锁更新：只有version匹配才更新成功
   productMapper.reserveStock(productId, quantity, product.getVersion());
   ```
   - stock字段：总库存
   - reservedStock字段：预扣库存
   - 可用库存 = stock - reservedStock

2. **确认扣减**（支付成功时）
   ```java
   productMapper.confirmStock(productId, quantity);
   ```
   - 将预扣库存正式扣减

3. **释放库存**（取消订单/超时）
   ```java
   productMapper.releaseReservedStock(productId, quantity);
   ```

**双重锁机制**：
- Redisson分布式锁：防止同一商品被多人同时下单
- 数据库乐观锁：防止并发更新问题

**缓存策略**：
- 库存缓存10秒过期，频繁操作时快速失效
- 缓存更新策略：删除而非更新，保证一致性

**数据库设计**（Product表）：
```sql
-- 核心字段
stock INT -- 总库存
reservedStock INT -- 预扣库存
version INT -- 乐观锁版本号
```

### 3.2 异步事件处理模块

**文件位置**：[AsyncEventProcessor.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\processor\AsyncEventProcessor.java)、[ReliableEventConsumer.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\processor\ReliableEventConsumer.java)、[ReliableEventProducer.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\processor\ReliableEventProducer.java)

**设计逻辑**：

使用Redis Stream实现可靠消息队列，支持：

1. **生产者**（发送事件）
   - 点赞事件、评论事件、浏览事件
   - 3次重试机制
   - 失败后写入死信队列

2. **消费者组**
   - 多个消费者并行消费
   - XACK确认机制，确保消息不丢失
   - Pending List处理超时未确认消息

3. **批量更新服务**（[BatchUpdateService.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\service\BatchUpdateService.java)）
   - 使用LongAdder做计数器，高并发下性能更好
   - 内存缓冲区，每10秒批量刷入数据库
   - 减少数据库写入压力

```java
// 核心数据结构
private final Map<String, LongAdder> likeCountBuffer = new ConcurrentHashMap<>();
```

**事件限流**：
- 浏览事件限流：同一用户对同一内容每分钟最多记录10次

**死信队列**（[DeadLetterService.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\processor\DeadLetterService.java)）：
- 存储失败消息
- 定时重试处理

### 3.3 多级缓存模块

**文件位置**：[MultiLevelCache.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\cache\MultiLevelCache.java)

**设计逻辑**：

L1 -> L2 -> DB 三级访问：
1. **L1：Caffeine本地缓存**
   - 最大10000条
   - 写入后5分钟过期
   - 记录命中率统计
   
2. **L2：Redis分布式缓存**
   - 可配置TTL
   - 支持批量操作
   
3. **DB：数据库兜底**

**优势**：
- 90%+请求命中本地缓存，性能极高
- 减少Redis网络开销
- 缓存预热与回写机制完善

### 3.4 热榜系统

**文件位置**：[HotRankServiceImpl.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\service\Impl\HotRankServiceImpl.java)

**设计逻辑**：

1. **热度计算算法**
   - 基于权重计算：浏览(0.3) < 点赞(1.0) < 收藏(1.2) < 评论(1.5) < 分享(2.0)
   - 时间衰减：牛顿冷却定律 `T(t) = T0 * e^(-λ * t/24)`
   - 配置化权重，支持动态调整

2. **数据结构**
   ```java
   // Redis ZSet存储
   ZSetOperations.TypedTuple<String> // member = type:id, score = 热度
   ```

3. **多维度榜单**
   - 总榜：`hot_rank:all`
   - 分类榜：`hot_rank:category:{category}`
   - 今日榜：`hot_rank:today`
   - 飙升榜：按热度增量排序

4. **异步更新**
   - 使用`@Async`注解异步记录用户行为
   - 不阻塞主流程

### 3.5 订单超时处理

**文件位置**：[OrderTimeoutScheduler.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\task\OrderTimeoutScheduler.java)

**设计逻辑**：

- 定时任务每分钟扫描一次超时订单
- 释放预扣库存
- 更新订单状态为"已取消"
- 清理相关缓存

### 3.6 健康检查

**文件位置**：[RedisHealthChecker.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\health\RedisHealthChecker.java)

**设计逻辑**：

- 每5秒Ping一次Redis
- 连续失败3次标记为不健康
- 连续成功2次恢复健康
- 支持熔断器模式

---

## 四、潜在问题与风险分析

### 4.1 已发现的问题

| 问题 | 严重程度 | 位置 | 说明 |
|-----|--------|------|------|
| **MySQL连接参数缺失** | 🔴 高 | [application.yaml](file:///d:\Code\campus%20-%201\src\main\resources\application.yaml#L8) | 缺少`allowPublicKeyRetrieval=true`，已修复 |
| **重复依赖声明** | 🟡 中 | [pom.xml](file:///d:\Code\campus%20-%201\pom.xml) | elasticsearch-rest-high-level-client重复声明，已清理 |
| **javax→jakarta迁移** | 🟡 中 | 多个文件 | Spring Boot 3.x需要使用jakarta.*包，已修复 |

### 4.2 潜在风险

| 风险 | 影响 | 解决方案 |
|-----|------|---------|
| **Redis单点故障** | 🔴 高 | 使用Redis主从+哨兵，或引入熔断降级机制 |
| **库存缓存不一致** | 🟡 中 | 采用最终一致性，配合数据库乐观锁兜底 |
| **消息丢失** | 🟡 中 | Redis Stream ACK机制 + 死信队列 + 定时重试 |
| **缓存雪崩** | 🟡 中 | 给TTL添加随机值，避免同时失效 |
| **缓存击穿** | 🟡 中 | 使用布隆过滤器拦截不存在的key |
| **热榜大Key** | 🟡 中 | 考虑分片或本地缓存副本 |
| **ES数据同步延迟** | 🟡 中 | 使用消息队列异步同步，提供降级方案 |
| **数据库连接泄露** | 🟡 中 | HikariCP有连接池管理，但需监控连接数 |

### 4.3 代码层面问题

1. **[StockCacheService.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\service\Impl\StockCacheService.java)**：
   - 注释重复（第78-83行、112-117行）
   - 日志过多，生产环境可考虑降级为debug

2. **[HotRankServiceImpl.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\service\Impl\HotRankServiceImpl.java)**：
   - 时间衰减算法在`recordAction`中定义但未被调用
   - `shouldUpdateDatabase`和`updateDatabaseHotScore`方法定义了但未使用
   - 存在死代码，建议清理

3. **[AsyncEventProcessor.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\processor\AsyncEventProcessor.java)** 与 **[ReliableEventConsumer.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\processor\ReliableEventConsumer.java)**：
   - 功能类似，存在重复代码
   - 建议合并或明确职责分工

---

## 五、未来优化方向

### 5.1 架构层面

1. **服务拆分**
   - 将订单、商品、用户等模块拆分为独立服务
   - 引入服务发现（Nacos/Eureka）
   - 引入API网关

2. **消息队列升级**
   - 考虑替换为RocketMQ/Kafka
   - 支持事务消息、顺序消息等高级特性

3. **读写分离**
   - 引入数据库主从复制
   - 使用ShardingSphere或MyCat做分库分表

### 5.2 性能优化

1. **缓存优化**
   - 引入布隆过滤器防止缓存穿透
   - 使用Redis Lua脚本保证原子操作
   - 多级缓存一致性优化

2. **数据库优化**
   - 关键表添加合适索引
   - 大表考虑分区或分库分表
   - SQL慢查询监控与优化

3. **接口优化**
   - 引入GraphQL减少网络请求
   - 关键接口添加数据预热
   - 接口聚合与分层

### 5.3 可靠性优化

1. **分布式事务**
   - 引入Seata解决跨服务事务问题
   - 补偿机制完善

2. **全链路追踪**
   - 引入SkyWalking/Sleuth
   - 日志关联TraceId

3. **监控告警**
   - Prometheus + Grafana
   - 关键指标（QPS、响应时间、错误率）告警

### 5.4 功能扩展

1. **推荐系统**
   - 基于协同过滤的商品推荐
   - 基于内容的帖子推荐

2. **风控系统**
   - 用户行为风控
   - 恶意刷单检测

3. **IM系统**
   - 买卖双方即时通讯
   - 消息推送

---

## 六、面试常见问题与回答

### Q1: 如何保证库存扣减不超卖？

**A:** 我使用了多重保障机制：

1. **数据库乐观锁**：在Product表中添加version字段，更新时使用乐观锁：
   ```sql
   UPDATE product SET reserved_stock = reserved_stock + #{quantity}, 
                      version = version + 1 
   WHERE id = #{id} AND version = #{version}
   ```

2. **Redisson分布式锁**：防止同一商品被多个请求同时处理：
   ```java
   RLock lock = redissonClient.getLock("product:lock:" + productId);
   boolean isLock = lock.tryLock(1, 5, TimeUnit.SECONDS);
   ```

3. **三阶段库存扣减**：预扣库存 → 确认扣减/释放库存，解决支付超时问题

4. **事务管理**：使用`@Transactional`保证操作原子性

---

### Q2: 如何处理高并发场景下的点赞/浏览操作？

**A:** 采用异步 + 批量处理方案：

1. **Redis Stream消息队列**：将点赞/浏览事件异步写入Stream，不阻塞主流程

2. **消费端批量处理**：
   ```java
   // 内存缓冲区
   private final Map<String, LongAdder> likeCountBuffer = new ConcurrentHashMap<>();
   
   // 每10秒批量刷入数据库
   @Scheduled(fixedDelay = 10000)
   public void scheduledFlush() {
       flushLikeCounts();
       flushViewCounts();
   }
   ```

3. **限流保护**：同一用户对同一内容每分钟最多记录10次浏览，防止恶意刷量

4. **可靠消息**：支持3次重试，失败后进入死信队列，定时任务扫描重试

---

### Q3: 热榜是如何实现的？有什么优化点？

**A:** 热榜设计如下：

1. **数据结构**：使用Redis ZSet，member为`type:id`，score为热度值

2. **热度计算**：
   - 权重配置：浏览0.3、点赞1.0、收藏1.2、评论1.5、分享2.0
   - 时间衰减：牛顿冷却定律，让旧内容热度自然降低

3. **性能优化**：
   - 异步更新：使用`@Async`记录用户行为，不阻塞主流程
   - 批量查询：从ZSet获取ID列表后，批量查询数据库，避免N+1问题

4. **优化空间**：
   - 热榜本地缓存副本，减轻Redis压力
   - 按时间分片存储（每小时/每天一个key）
   - 使用Redis Cluster防止大Key问题

---

### Q4: 缓存一致性如何保证？

**A:** 采用多级缓存策略：

1. **更新策略**：先更新数据库，再删除缓存（而非更新缓存）
   ```java
   // 删除缓存，让下次查询重新加载
   stringRedisTemplate.delete(key);
   ```

2. **多级缓存**：
   - L1：Caffeine本地缓存（5分钟过期）
   - L2：Redis分布式缓存（可配置TTL）
   - 删除缓存时同时删除两级

3. **最终一致性**：
   - 容忍短暂不一致
   - 配合数据库乐观锁兜底
   - 定时任务校准数据

4. **缓存更新保护**：
   - 库存缓存设置较短TTL（10秒）
   - 防止数据不一致时间过长

---

### Q5: 如何防止缓存雪崩、击穿、穿透？

**A:** 各种问题的解决方案：

1. **缓存雪崩**（大量key同时失效）：
   - 给TTL添加随机值：`expire = 30min + Random(0-5min)`
   - 使用队列或锁保证缓存重建的串行化

2. **缓存击穿**（热点key失效）：
   - 热点key永不过期
   - 使用互斥锁，只有一个线程去重建缓存
   - 本地缓存副本

3. **缓存穿透**（查询不存在的数据）：
   - 布隆过滤器拦截不存在的key
   - 缓存空值，设置较短过期时间

---

### Q6: 谈谈项目中消息队列的设计？

**A:** 使用Redis Stream做消息队列，设计要点：

1. **可靠性保证**：
   - 生产者端：3次重试 + 死信队列
   - 消费者端：消费者组 + XACK确认 + Pending List重试
   - 消息ID：UUID + 时间戳，保证幂等

2. **消费模式**：
   - 使用消费者组实现负载均衡
   - 多消费者并行消费，提高吞吐量
   - Pending消息定时扫描，超过5分钟未确认的重新投递

3. **死信队列**：
   - 消费失败超过3次的消息写入数据库
   - 定时任务扫描死信表，手动或自动重试

4. **可观测性**：
   - 记录消息发送/消费日志
   - 监控Lag（积压消息数）

---

### Q7: 分布式锁是如何实现的？有什么注意事项？

**A:** 使用Redisson实现分布式锁：

1. **核心代码**：
   ```java
   RLock lock = redissonClient.getLock("product:lock:" + productId);
   try {
       boolean isLock = lock.tryLock(1, 5, TimeUnit.SECONDS); // 等待1秒，锁持有5秒
       if (!isLock) {
           return Result.fail("系统繁忙");
       }
       // 业务逻辑
   } finally {
       if (lock.isHeldByCurrentThread()) {
           lock.unlock();
       }
   }
   ```

2. **Redisson优势**：
   - 自动续期（看门狗机制）：防止业务未执行完锁就过期
   - 可重入锁
   - 支持红锁（RedLock）

3. **注意事项**：
   - 加锁和解锁必须在同一个线程
   - 使用finally确保锁释放
   - 设置合理的锁等待时间和持有时间

---

### Q8: 订单超时是如何处理的？

**A:** 订单超时处理方案：

1. **数据库字段设计**：
   ```sql
   expire_time DATETIME -- 订单过期时间
   status INT -- 0:待支付 1:已支付 2:已取消
   ```

2. **定时任务扫描**：
   ```java
   @Scheduled(cron = "0 * * * * ?") // 每分钟执行
   public void processTimeoutOrders() {
       List<Order> timeoutOrders = orderMapper.selectTimeoutOrders();
       for (Order order : timeoutOrders) {
           // 释放预扣库存
           productMapper.releaseReservedStock(order.getProductId(), order.getQuantity());
           // 更新订单状态
           orderMapper.updateStatus(order.getId(), 2);
       }
   }
   ```

3. **支付时二次校验**：
   ```java
   if (order.getExpireTime().isBefore(LocalDateTime.now())) {
       // 超时订单，先释放库存再返回失败
       productMapper.releaseReservedStock(order.getProductId(), order.getQuantity());
       return Result.fail("订单已超时");
   }
   ```

4. **优化方向**：
   - 可以使用RabbitMQ延迟队列，精度更高
   - 或使用Redis Keyspace Notifications

---

### Q9: Elasticsearch是如何使用的？

**A:** ES主要用于商品和帖子的全文搜索：

1. **索引设计**：
   - 商品索引：`campus_product`
   - 帖子索引：`campus_post`

2. **数据同步**：
   - 商品/帖子创建/更新时，异步同步到ES
   - 使用消息队列解耦

3. **搜索功能**：
   - 支持关键词搜索（标题、描述）
   - 支持分类过滤
   - 支持分页排序

4. **问题处理**：
   - ES不可用时降级到数据库模糊查询
   - 保证核心功能可用

---

### Q10: 项目中还有哪些可以改进的地方？

**A:** 以下是一些改进建议：

1. **代码层面**：
   - 去除重复代码（AsyncEventProcessor和ReliableEventConsumer）
   - 统一异常处理
   - 添加更多单元测试

2. **架构层面**：
   - 引入配置中心（Nacos/Apollo）
   - 引入服务治理（Sentinel）
   - 读写分离

3. **运维层面**：
   - Docker容器化部署
   - K8s编排
   - 完善的监控告警

4. **功能层面**：
   - 引入分布式事务（Seata）
   - 添加用户画像和推荐系统
   - 增加更多风控规则

---

## 七、总结

这是一个设计相对完善的校园论坛二手交易系统，核心亮点在于：

✅ **库存管理**：乐观锁 + 分布式锁 + 三阶段扣减，高并发下不超卖  
✅ **异步事件**：Redis Stream + 批量更新 + 死信队列，高性能高可靠  
✅ **多级缓存**：Caffeine + Redis，性能与一致性平衡  
✅ **热榜系统**：Redis ZSet + 时间衰减算法，实时高效  

但也存在一些可以优化的地方，如代码重复、功能未启用等，可在后续迭代中逐步完善。

整体而言，这是一个很好的项目案例，覆盖了高并发场景下的多个核心技术点，非常适合作为面试项目展示。
