# 拦截器模块详解

## 目录

1. [模块概述](#一模块概述)
2. [核心拦截器详解](#二核心拦截器详解)
3. [拦截器链配置](#三拦截器链配置)
4. [ThreadLocal设计](#四threadlocal设计)
5. [技术亮点](#五技术亮点)
6. [面试常见问题](#六面试常见问题)
7. [优化建议](#七优化建议)

---

## 一、模块概述

### 1.1 功能介绍

本项目通过**三层拦截器**实现完整的请求处理流程：

| 拦截器 | 优先级 | 功能 |
|------|--------|------|
| **RateLimitInterceptor** | 0 | 限流保护，防止恶意刷接口 |
| **RefreshTokenInterceptor** | 0 | 刷新Token有效期，自动续期 |
| **LoginInterceptor** | 1 | 登录校验，保护需认证接口 |

### 1.2 拦截器链执行顺序

```
请求到达
    ↓
[RateLimitInterceptor] 限流检查 → 拦截/放行
    ↓
[RefreshTokenInterceptor] 刷新Token有效期 → 保存用户到ThreadLocal
    ↓
[LoginInterceptor] 登录检查 → 拦截/放行
    ↓
Controller执行业务
    ↓
[RefreshTokenInterceptor.afterCompletion] 清理ThreadLocal
    ↓
响应返回
```

---

## 二、核心拦截器详解

### 2.1 限流拦截器 `RateLimitInterceptor`

**文件位置**：[RateLimitInterceptor.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\interceptor\RateLimitInterceptor.java)

#### 核心功能

- 检查方法是否有`@RateLimit`注解
- 提取限流参数（容量、补充速率）
- 调用Lua脚本原子执行令牌桶逻辑
- 根据结果决定放行或拦截

#### 核心代码

```java
@Override
public boolean preHandle(HttpServletRequest request,
                         HttpServletResponse response,
                         Object handler) throws Exception {
    
    // 1. 检查是否有@RateLimit注解
    if (!(handler instanceof HandlerMethod)) return true;
    
    HandlerMethod handlerMethod = (HandlerMethod) handler;
    RateLimit rateLimit = handlerMethod.getMethodAnnotation(RateLimit.class);
    
    if (rateLimit == null) return true;  // 没加注解，直接放行
    
    // 2. 提取参数
    long capacity = rateLimit.capacity();
    long refillRate = rateLimit.refillRate();
    
    // 3. 生成Key
    Long userId = StudentHolder.getStudentId();
    String key = "rate:token:" + (userId != null ? userId : "anonymous") + ":" + requestUri;
    
    // 4. 执行Lua脚本
    List<Long> result = stringRedisTemplate.execute(
        redisScript,
        Arrays.asList(key),
        capacity, refillRate, now, "1"
    );
    
    // 5. 裁决
    if (result.get(0) == 0) {
        renderErrorResponse(response, rateLimit.message());
        return false;
    }
    
    return true;
}
```

#### 限流返回

```json
{
  "success": false,
  "errorMsg": "请求过于频繁，请稍后再试"
}
```
HTTP状态码：429

---

### 2.2 Token刷新拦截器 `RefreshTokenInterceptor`

**文件位置**：[RefreshTokenInterceptor.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\interceptor\RefreshTokenInterceptor.java)

#### 核心功能

- 从请求头获取Token
- 从Redis查询用户信息
- 将用户保存到ThreadLocal
- 刷新Token有效期（自动续期）
- 请求结束后清理ThreadLocal

#### 核心代码

```java
@Override
public boolean preHandle(HttpServletRequest request,
                         HttpServletResponse response,
                         Object handler) throws Exception {
    
    // 1. 从请求头获取token
    String token = request.getHeader("authorization");
    if (StrUtil.isBlank(token)) {
        return true;  // 没有token，放行，交给后面的LoginInterceptor处理
    }
    
    // 2. 从Redis查询用户
    String key = LOGIN_USER_KEY + token;
    Map<Object, Object> userMap = stringRedisTemplate.opsForHash().entries(key);
    
    if (userMap.isEmpty()) {
        return true;  // Redis里没找到，放行
    }
    
    // 3. 将用户保存到ThreadLocal
    StudentDTO studentDTO = BeanUtil.fillBeanWithMap(userMap, new StudentDTO(), false);
    studentDTO.setToken(token);
    StudentHolder.saveStudent(studentDTO);
    
    // 4. 刷新Token有效期（自动续期30分钟）
    stringRedisTemplate.expire(key, LOGIN_USER_TTL, TimeUnit.MINUTES);
    
    return true;
}

@Override
public void afterCompletion(HttpServletRequest request,
                            HttpServletResponse response,
                            Object handler, Exception ex) throws Exception {
    // 5. 请求结束，清理ThreadLocal，防止内存泄漏
    StudentHolder.removeStudent();
}
```

#### 设计亮点

| 点 | 说明 |
|---|------|
| **自动续期** | 用户每次请求都刷新Token有效期，保持登录状态 |
| **ThreadLocal** | 线程隔离，避免多线程数据混乱 |
| **afterCompletion清理** | 防止内存泄漏，用完及时清理 |

---

### 2.3 登录拦截器 `LoginInterceptor`

**文件位置**：[LoginInterceptor.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\interceptor\LoginInterceptor.java)

#### 核心功能

- 检查ThreadLocal中是否有用户
- 有用户：放行
- 没用户：拦截，返回401

#### 核心代码

```java
@Override
public boolean preHandle(HttpServletRequest request,
                         HttpServletResponse response,
                         Object handler) throws Exception {
    
    // 判断ThreadLocal中是否有用户
    if (StudentHolder.getStudent() == null) {
        response.setStatus(401);
        response.setContentType("application/json;charset=UTF-8");
        response.getWriter().write("{\"success\":false,\"errorMsg\":\"请先登录\"}");
        return false;
    }
    
    return true;
}
```

#### 白名单接口

在`MvcConfig`中配置了无需登录的接口：

```java
// 静态资源
"/**/*.html",
"/**/*.css",
"/**/*.js",
// ...

// 用户相关公开接口
"/student/code",        // 发送验证码
"/student/login",       // 登录
"/student/register",    // 注册

// 帖子相关公开接口
"/post/hot",
"/post/category",
"/post/*",

// 商品相关公开接口
"/product/category",
"/product/*",

// 热榜公开接口
"/hot/**",

// 文件上传
"/upload/**"
```

#### 登录失败返回

```json
{
  "success": false,
  "errorMsg": "请先登录"
}
```
HTTP状态码：401

---

## 三、拦截器链配置

**文件位置**：[MvcConfig.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\config\MvcConfig.java)

### 3.1 配置代码

```java
@Configuration
public class MvcConfig implements WebMvcConfigurer {
    
    @Resource
    private StringRedisTemplate stringRedisTemplate;
    
    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        
        // 1. 限流拦截器（优先级最高，order=0）
        registry.addInterceptor(new RateLimitInterceptor(stringRedisTemplate))
                .addPathPatterns("/**")
                .order(0);
        
        // 2. Token刷新拦截器（优先级同样高，order=0）
        registry.addInterceptor(new RefreshTokenInterceptor(stringRedisTemplate))
                .addPathPatterns("/**")
                .order(0);
        
        // 3. 登录拦截器（优先级较低，order=1）
        registry.addInterceptor(new LoginInterceptor(stringRedisTemplate))
                .excludePathPatterns(
                    // 白名单接口
                    "/student/code",
                    "/student/login",
                    "/post/hot",
                    // ...
                )
                .order(1);
    }
}
```

### 3.2 优先级说明

| order值 | 说明 |
|---------|------|
| **0** | 最先执行，限流和Token刷新并行 |
| **1** | 后执行，登录校验 |

注意：相同order的拦截器，按添加顺序执行。

---

## 四、ThreadLocal设计

### 4.1 StudentHolder工具类

**文件位置**：[StudentHolder.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\utils\StudentHolder.java)

```java
public class StudentHolder {
    
    private static final ThreadLocal<StudentDTO> tl = new ThreadLocal<>();
    
    // 保存用户
    public static void saveStudent(StudentDTO studentDTO) {
        tl.set(studentDTO);
    }
    
    // 获取用户
    public static StudentDTO getStudent() {
        return tl.get();
    }
    
    // 获取用户ID
    public static Long getStudentId() {
        StudentDTO student = tl.get();
        return student == null ? null : student.getId();
    }
    
    // 清理ThreadLocal
    public static void removeStudent() {
        tl.remove();
    }
}
```

### 4.2 ThreadLocal的作用

| 问题 | 解决方式 |
|------|---------|
| **多线程数据混乱** | ThreadLocal线程隔离，每个线程有自己的副本 |
| **层层传递用户信息** | 直接从ThreadLocal获取，不用在方法参数传来传去 |
| **内存泄漏** | `afterCompletion`中及时清理 |

### 4.3 使用示例

在Controller中直接获取用户：

```java
@PostMapping("/like/{id}")
@RateLimit(value = 5, timeout = 1, message = "点赞太频繁")
public Result likePost(@PathVariable Long id) {
    // 直接从ThreadLocal获取用户
    Long userId = StudentHolder.getStudentId();
    
    likeService.like("post", id);
    return Result.ok();
}
```

---

## 五、技术亮点

### 5.1 双重拦截器设计（Refresh + Login）

| 优势 | 说明 |
|------|------|
| **自动续期** | 用户每次请求都刷新Token有效期 |
| **灵活性** | 公开接口也能获取用户信息（比如显示"你好，XXX"） |
| **解耦** | Token刷新和登录检查分离，职责单一 |

### 5.2 线程安全的用户信息存储

- ThreadLocal保证线程隔离
- `afterCompletion`保证及时清理，防止内存泄漏

### 5.3 白名单配置

- 静态资源放行
- 公开接口（首页、商品列表）无需登录
- 核心接口（发帖子、点赞）需要登录

### 5.4 限流优先

- 先限流，再做其他检查
- 防止恶意请求消耗资源

---

## 六、面试常见问题

### 问题1：为什么需要两个拦截器（Refresh和Login）？

**面试官考察点**：
- 是否理解设计的灵活性
- 是否知道分层设计的好处

**回答参考**：

> 主要是为了**灵活性**：
>
> 1. **自动续期**：用户每次请求都能刷新Token有效期，保持登录状态
> 2. **公开接口也能获取用户**：比如首页虽然不用登录，但如果用户登录了，可以显示"你好，XXX"
> 3. **职责分离**：Token刷新和登录检查分开，代码更清晰
>
> 如果只有一个LoginInterceptor，就做不到自动续期了。

---

### 问题2：ThreadLocal内存泄漏怎么解决？

**面试官考察点**：
- 是否理解ThreadLocal内存泄漏的原因
- 是否知道怎么避免

**回答参考**：

> ThreadLocal内存泄漏是因为：
> - ThreadLocalMap的Key是WeakReference，Value是强引用
> - 如果线程不结束，Value就不会被回收
>
> 我们的解决方式是在`RefreshTokenInterceptor.afterCompletion`中调用：
> ```java
> StudentHolder.removeStudent();
> ```
> 每次请求结束后都清理，避免内存泄漏。
>
> 另外，项目是Web应用，线程池里的线程会复用，更要及时清理，否则会数据混乱。

---

### 问题3：拦截器的执行顺序怎么定的？

**面试官考察点**：
- 是否理解Spring MVC拦截器的order机制
- 是否知道相同order的执行顺序

**回答参考**：

> 通过`order()`方法设置优先级：
> - order值**越小**，优先级**越高**，越先执行
>
> 我们的配置：
> ```java
> .addInterceptor(new RateLimitInterceptor(...)).order(0)
> .addInterceptor(new RefreshTokenInterceptor(...)).order(0)
> .addInterceptor(new LoginInterceptor(...)).order(1)
> ```
>
> 相同order的，按添加顺序执行：先限流，再刷新Token，最后检查登录。
>
> 限流放最前面是为了优先拦截恶意请求，不浪费资源。

---

### 问题4：拦截器和过滤器（Filter）有什么区别？

**面试官考察点**：
- 是否理解Web层组件的层次
- 是否知道什么时候用哪个

**回答参考**：

| 维度 | Filter | Interceptor |
|------|--------|-------------|
| **规范** | Servlet规范 | Spring框架 |
| **执行时机** | 请求进入Servlet之前 | 请求到达Controller之前、之后 |
| **功能** | 编码、安全、跨域 | 登录检查、限流、日志 |
| **访问Controller** | 不能 | 能（可以获取HandlerMethod、注解等） |
| **获取Spring容器Bean** | 比较麻烦 | 可以直接注入 |

我们用拦截器是因为：
- 需要访问Spring容器的Bean（StringRedisTemplate）
- 需要检查Controller方法上的注解（@RateLimit）
- 需要在Controller执行前后做处理

---

### 问题5：白名单接口是怎么配置的？

**面试官考察点**：
- 是否知道Spring MVC拦截器的excludePathPatterns
- 是否考虑过路径匹配规则

**回答参考**：

> 通过`excludePathPatterns()`配置：
> ```java
> .excludePathPatterns(
>     // 静态资源
>     "/**/*.html", "/**/*.css", "/**/*.js",
>     // 公开接口
>     "/student/code", "/student/login",
>     "/post/hot", "/post/category",
>     "/hot/**"
> )
> ```
>
> 路径匹配用的是Ant风格：
> - `*` 匹配单级路径
> - `**` 匹配多级路径

---

### 问题6：怎么在Controller中获取当前登录用户？

**面试官考察点**：
- 是否知道ThreadLocal的使用
- 是否知道怎么优雅地传递用户信息

**回答参考**：

> 通过`StudentHolder`工具类从ThreadLocal获取：
> ```java
> @PostMapping("/post")
> public Result createPost(@RequestBody Post post) {
>     // 直接从ThreadLocal获取
>     Long userId = StudentHolder.getStudentId();
>     post.setUserId(userId);
>     
>     postService.save(post);
>     return Result.ok();
> }
> ```
>
> 这样不用在方法参数传来传去，代码更优雅。

---

### 问题7：Token是怎么刷新的？

**面试官考察点**：
- 是否理解Token自动续期的设计
- 是否知道Redis的expire操作

**回答参考**：

> 在`RefreshTokenInterceptor`中：
> ```java
> // 每次请求都刷新有效期为30分钟
> stringRedisTemplate.expire(key, LOGIN_USER_TTL, TimeUnit.MINUTES);
> ```
>
> 这样用户只要持续使用，就不会掉线，体验更好。
>
> 如果用户30分钟不操作，Token自动过期，需要重新登录，安全性也有保障。

---

### 问题8：多个拦截器的preHandle和afterCompletion执行顺序是怎样的？

**面试官考察点**：
- 是否理解Spring MVC拦截器的完整执行流程

**回答参考**：

> **preHandle**：按order顺序**正向执行**（从小到大）
> - RateLimit → RefreshToken → Login → Controller
>
> **afterCompletion**：按order顺序**反向执行**（从大到小）
> - Controller → Login → RefreshToken → RateLimit
>
> 举个例子：
> ```
> 请求 → RateLimit.pre → RefreshToken.pre → Login.pre → Controller
>                                                         ↓
> 响应 ← RateLimit.after ← RefreshToken.after ← Login.after ←
> ```
>
> 所以我们把清理ThreadLocal的逻辑放在RefreshToken的afterCompletion，确保最后清理。

---

### 问题9：如果有10个白名单接口，怎么配置更优雅？

**面试官考察点**：
- 是否有代码优化意识
- 是否知道用配置文件或常量管理

**回答参考**：

> 当前是直接写死在代码里，可以优化：
>
> 1. **用常量类**：
> ```java
> public class WebConstants {
>     public static final List<String> WHITE_LIST = Arrays.asList(
>         "/student/code",
>         "/student/login",
>         // ...
>     );
> }
> ```
> 然后：
> ```java
> .excludePathPatterns(WebConstants.WHITE_LIST)
> ```
>
> 2. **用配置文件**（推荐）：
> ```yaml
> web:
>   white-list:
>     - /student/code
>     - /student/login
> ```
> 然后：
> ```java
> @Value("${web.white-list}")
> private List<String> whiteList;
> ```
>
> 这样可以动态调整，不用改代码。

---

### 问题10：拦截器里怎么注入Bean？

**面试官考察点**：
- 是否理解Spring Bean的注入方式
- 是否知道拦截器不能用@Autowired的原因

**回答参考**：

> 普通的拦截器如果用`new`创建的，不能用`@Autowired`，因为不在Spring容器里。
>
> 我们的解决方案是在`MvcConfig`中注入Bean，然后通过构造函数传给拦截器：
> ```java
> @Resource
> private StringRedisTemplate stringRedisTemplate;
> 
> @Override
> public void addInterceptors(InterceptorRegistry registry) {
>     registry.addInterceptor(new RefreshTokenInterceptor(stringRedisTemplate))
>             // ...
> }
> ```
> 拦截器里通过构造函数接收：
> ```java
> public class RefreshTokenInterceptor {
>     private final StringRedisTemplate stringRedisTemplate;
>     
>     public RefreshTokenInterceptor(StringRedisTemplate stringRedisTemplate) {
>         this.stringRedisTemplate = stringRedisTemplate;
>     }
> }
> ```
>
> 另一种方式是把拦截器做成Bean，但我觉得构造函数传参更清晰。

---

## 七、优化建议

### 7.1 短期优化

1. **白名单配置化**：从配置文件读取，不用改代码
2. **添加拦截器日志**：记录请求路径、用户、耗时，方便排查问题
3. **限流监控**：记录被限流的请求，分析哪些接口被限流最多

### 7.2 长期优化

1. **引入Spring Security**：更成熟的安全框架，支持权限控制
2. **分布式会话共享**：如果是多实例部署，用Redis共享Session
3. **统一异常处理**：拦截器里的异常可以用`@ExceptionHandler`统一处理
4. **限流降级**：限流后可以返回降级数据，而不是直接报错

---

## 总结

本项目通过三层拦截器实现了完整的请求处理流程：
- **RateLimitInterceptor**：限流保护
- **RefreshTokenInterceptor**：刷新Token，保存用户到ThreadLocal
- **LoginInterceptor**：登录检查

配合ThreadLocal实现了线程安全的用户信息传递，设计清晰、职责分离！

---

**文档版本**：v1.0  
**最后更新**：2026-05-30
