# 订单下单唯一性一致性设计模块详解

## 目录

1. [模块概述](#1-模块概述)
2. [数据库设计](#2-数据库设计)
3. [核心流程](#3-核心流程)
4. [技术实现](#4-技术实现)
5. [面试常见问题](#5-面试常见问题)
6. [扩展场景题目](#6-扩展场景题目)

---

## 1. 模块概述

### 1.1 核心问题
在高并发下单场景下，需要保证：
- **唯一性**：同一用户重复点击下单，只创建一个订单
- **一致性**：库存扣减和订单创建原子性，不超卖
- **可靠性**：订单超时、取消时能正确释放库存

### 1.2 设计原则
| 原则 | 说明 |
|------|------|
| **双重锁机制** | Redisson分布式锁 + 数据库乐观锁 |
| **三阶段库存扣减** | 预扣 → 确认/释放 |
| **订单唯一标识** | 幂等性保证 |
| **超时自动取消** | 定时任务清理 |

---

## 2. 数据库设计

### 2.1 订单表 tb_order
```sql
CREATE TABLE tb_order (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_no VARCHAR(64) UNIQUE COMMENT '订单号',
    product_id BIGINT COMMENT '商品ID',
    user_id BIGINT COMMENT '用户ID',
    quantity INT COMMENT '购买数量',
    amount DECIMAL(10,2) COMMENT '订单金额',
    status INT DEFAULT 0 COMMENT '0-待支付 1-已支付 2-已取消 3-已完成',
    expire_time DATETIME COMMENT '过期时间',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    pay_time DATETIME,
    INDEX idx_user_id (user_id),
    INDEX idx_order_no (order_no)
);
```

### 2.2 商品表 tb_product（库存相关字段）
```sql
CREATE TABLE tb_product (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    stock INT COMMENT '总库存',
    reserved_stock INT DEFAULT 0 COMMENT '预扣库存',
    version INT DEFAULT 0 COMMENT '乐观锁版本号',
    -- 可用库存 = stock - reserved_stock
    -- 其他字段...
);
```

**关键设计点**：
- `reserved_stock`：预扣库存，下单时增加，支付时减少
- `version`：乐观锁版本号，防止并发更新

---

## 3. 核心流程

### 3.1 创建订单（预扣库存）
```
用户点击下单
    ↓
获取Redisson分布式锁 (product:lock:{productId})
    ↓
查询商品（带version）
    ↓
检查可用库存 (stock - reservedStock)
    ↓
乐观锁预扣库存
    UPDATE tb_product 
    SET reserved_stock = reserved_stock + quantity,
        version = version + 1
    WHERE id = productId 
      AND (stock - reserved_stock) >= quantity
      AND version = #{version}
    ↓
删除库存缓存
    ↓
创建订单（status=0待支付）
    ↓
释放锁
    ↓
返回成功
```

**文件位置**：[OrderServiceImpl.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\service\Impl\OrderServiceImpl.java#L62-L122)

### 3.2 支付订单（确认扣减库存）
```
用户点击支付
    ↓
查询订单
    ↓
检查是否超时
    ↓
确认扣减库存
    UPDATE tb_product 
    SET stock = stock - quantity,
        reserved_stock = reserved_stock - quantity
    WHERE id = productId 
      AND reserved_stock >= quantity
    ↓
删除库存缓存
    ↓
更新订单状态为已支付
    ↓
返回成功
```

**文件位置**：[OrderServiceImpl.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\service\Impl\OrderServiceImpl.java#L128-L171)

### 3.3 取消订单（释放预扣库存）
```
用户取消订单 / 订单超时
    ↓
查询订单
    ↓
检查订单状态（必须是待支付）
    ↓
释放预扣库存
    UPDATE tb_product 
    SET reserved_stock = reserved_stock - quantity
    WHERE id = productId 
      AND reserved_stock >= quantity
    ↓
删除库存缓存
    ↓
更新订单状态为已取消
    ↓
返回成功
```

**文件位置**：[OrderServiceImpl.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\service\Impl\OrderServiceImpl.java#L177-L214)

### 3.4 订单超时自动取消
```
定时任务（每分钟执行）
    ↓
查询超时订单（expireTime < NOW）
    ↓
逐个处理：
    释放预扣库存
    删除库存缓存
    更新订单状态为已取消
```

**文件位置**：[OrderTimeoutScheduler.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\task\OrderTimeoutScheduler.java)

---

## 4. 技术实现

### 4.1 核心代码：创建订单

```java
@Override
@Transactional
public Result createOrder(Long productId, Integer quantity) {
    Long userId = StudentHolder.getStudentId();
    
    // 1. 获取分布式锁
    String lockKey = "product:lock:" + productId;
    RLock lock = redissonClient.getLock(lockKey);
    
    try {
        boolean isLock = lock.tryLock(1, 5, TimeUnit.SECONDS);
        if (!isLock) {
            return Result.fail("系统繁忙，请稍后再试");
        }
        
        // 2. 查询商品（带乐观锁版本号）
        Product product = productMapper.selectForUpdate(productId);
        if (product == null) {
            return Result.fail("商品不存在");
        }
        
        // 3. 检查库存
        int availableStock = product.getStock() - product.getReservedStock();
        if (availableStock < quantity) {
            return Result.fail("库存不足");
        }
        
        // 4. 乐观锁预扣库存
        int rows = productMapper.reserveStock(
            productId, quantity, product.getVersion());
        if (rows == 0) {
            return Result.fail("下单失败，请重试");
        }
        
        // 5. 删除缓存，保证一致性
        stockCacheService.onReserveStock(productId, quantity);
        
        // 6. 创建订单
        Order order = new Order();
        order.setOrderNo(generateOrderNo());  // 唯一订单号
        order.setProductId(productId);
        order.setUserId(userId);
        order.setQuantity(quantity);
        order.setStatus(0);  // 待支付
        order.setExpireTime(LocalDateTime.now().plusMinutes(30));
        orderMapper.insert(order);
        
        return Result.ok(order);
        
    } finally {
        if (lock.isHeldByCurrentThread()) {
            lock.unlock();
        }
    }
}
```

**文件位置**：[OrderServiceImpl.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\service\Impl\OrderServiceImpl.java#L62-L122)

### 4.2 核心代码：数据库乐观锁

```java
// ProductMapper.java

/**
 * 预扣库存（下单时调用）
 */
@Update("UPDATE tb_product " +
        "SET reserved_stock = reserved_stock + #{quantity}, version = version + 1 " +
        "WHERE id = #{id} " +
        "AND (stock - reserved_stock) >= #{quantity} " +
        "AND version = #{version}")
int reserveStock(@Param("id") Long id,
                 @Param("quantity") Integer quantity,
                 @Param("version") Integer version);

/**
 * 确认扣减库存（支付成功时调用）
 */
@Update("UPDATE tb_product " +
        "SET stock = stock - #{quantity}, reserved_stock = reserved_stock - #{quantity} " +
        "WHERE id = #{id} " +
        "AND reserved_stock >= #{quantity}")
int confirmStock(@Param("id") Long id,
                 @Param("quantity") Integer quantity);

/**
 * 释放预扣库存（订单取消/超时时调用）
 */
@Update("UPDATE tb_product " +
        "SET reserved_stock = reserved_stock - #{quantity} " +
        "WHERE id = #{id} " +
        "AND reserved_stock >= #{quantity}")
int releaseReservedStock(@Param("id") Long id,
                         @Param("quantity") Integer quantity);
```

**文件位置**：[ProductMapper.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\mapper\ProductMapper.java#L19-L47)

### 4.3 库存缓存更新策略

```java
// StockCacheService.java

/**
 * 预扣库存时：删除缓存，让下次查询重新加载
 */
public void onReserveStock(Long productId, Integer quantity) {
    String stockKey = CACHE_STOCK_KEY + productId;
    stringRedisTemplate.delete(stockKey);  // 删除而非更新
    multiLevelCache.evict(CACHE_PRODUCT_KEY + productId);
}

/**
 * 确认扣减时：同样删除缓存
 */
public void onConfirmStock(Long productId, Integer quantity) {
    String stockKey = CACHE_STOCK_KEY + productId;
    stringRedisTemplate.delete(stockKey);
    multiLevelCache.evict(CACHE_PRODUCT_KEY + productId);
}

/**
 * 释放库存时：删除缓存
 */
public void onReleaseStock(Long productId, Integer quantity) {
    String stockKey = CACHE_STOCK_KEY + productId;
    stringRedisTemplate.delete(stockKey);
    multiLevelCache.evict(CACHE_PRODUCT_KEY + productId);
}
```

**文件位置**：[StockCacheService.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\service\Impl\StockCacheService.java#L84-L128)

### 4.4 订单号生成

```java
/**
 * 生成订单号：时间戳 + 6位随机数
 * 格式：yyyyMMddHHmmss + 6位随机数
 */
private String generateOrderNo() {
    String timestamp = LocalDateTime.now().format(
        DateTimeFormatter.ofPattern("yyyyMMddHHmmss"));
    String random = RandomUtil.randomNumbers(6);
    return timestamp + random;
}
```

**文件位置**：[OrderServiceImpl.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\service\Impl\OrderServiceImpl.java#L323-L327)

---

## 5. 面试常见问题

### 问题1：怎么保证不超卖？

**面试官考察点**：双重锁机制的理解

**回答参考**：
> 通过**双重锁机制**保证：
> 
> 1. **Redisson分布式锁**：防止同一商品被多个请求同时下单
>    ```java
>    String lockKey = "product:lock:" + productId;
>    RLock lock = redissonClient.getLock(lockKey);
>    lock.tryLock(1, 5, TimeUnit.SECONDS);
>    ```
> 
> 2. **数据库乐观锁**：防止同一商品被多个请求同时更新
>    ```sql
>    UPDATE tb_product 
>    SET reserved_stock = reserved_stock + #{quantity}, 
>        version = version + 1 
>    WHERE id = #{id} 
>      AND (stock - reserved_stock) >= #{quantity} 
>      AND version = #{version}
>    ```
> 
> 分布式锁保证同一商品只有一个请求进入处理，乐观锁保证数据库层面的并发安全，双重保障。

---

### 问题2：为什么需要三阶段库存扣减（预扣、确认、释放）？

**面试官考察点**：超时订单处理的设计

**回答参考**：
> 因为用户下单后可能不支付，所以需要：
> 
> 1. **预扣库存**：下单时锁定库存，防止被其他人买走
>    - 增加 `reserved_stock`
>    - 可用库存 = `stock - reserved_stock`
> 
> 2. **确认扣减**：支付成功时真正扣减库存
>    - 减少 `stock` 和 `reserved_stock`
> 
> 3. **释放库存**：订单超时或取消时释放预扣的库存
>    - 减少 `reserved_stock`
> 
> 这样既保证了用户体验（下单即锁定库存），又不会因为用户不支付导致库存浪费。

---

### 问题3：怎么防止同一用户重复下单？

**面试官考察点**：幂等性设计

**回答参考**：
> 通过**订单号唯一性**保证：
> 
> 1. 前端生成幂等Token，下单时带上
> 2. 后端检查Redis中是否有该Token
> 3. 如果有，说明是重复请求，直接返回已有订单
> 4. 如果没有，创建订单并将Token存入Redis
> 
> 或者：
> - 在订单表上建唯一索引（user_id + product_id + status=0）
> - 防止同一用户对同一商品创建多个待支付订单
> 
> 当前项目中订单号是唯一的，可以基于订单号做幂等。

---

### 问题4：库存缓存怎么保证一致性？

**面试官考察点**：缓存更新策略

**回答参考**：
> 采用**删除缓存而非更新缓存**的策略：
> 
> ```java
> // 库存变动时：直接删除缓存
> stringRedisTemplate.delete(stockKey);
> ```
> 
> 这样下次查询时会重新从数据库加载最新数据。
> 
> 同时设置较短的过期时间（10秒），即使缓存删除失败，最多10秒后也会自动失效，保证最终一致性。

---

### 问题5：订单超时怎么处理？

**面试官考察点**：定时任务设计

**回答参考**：
> 通过**定时任务**处理：
> 
> ```java
> // 每分钟扫描一次超时订单
> @Scheduled(cron = "0 * * * * ?")
> public void processTimeoutOrders() {
>     List<Order> timeoutOrders = orderMapper.selectTimeoutOrders();
>     for (Order order : timeoutOrders) {
>         // 释放预扣库存
>         productMapper.releaseReservedStock(...);
>         // 更新订单状态
>         orderMapper.updateStatus(orderId, 2);
>     }
> }
> ```
> 
> 同时在用户支付时也会检查是否超时，如果超时则释放库存并返回失败。
> 
> 定时任务 + 支付时双重检查，保证超时订单一定被处理。

---

### 问题6：Redisson锁和数据库锁有什么区别？

**面试官考察点**：分布式锁 vs 数据库锁

**回答参考**：
> | 特性 | Redisson分布式锁 | 数据库乐观锁 |
> |------|-----------------|-------------|
> | **粒度** | 应用层 | 数据库层 |
> | **性能** | 高（Redis） | 较低（数据库） |
> | **作用** | 防止并发进入 | 防止并发更新 |
> | **粒度** | 商品级别 | 行级别 |
> 
> 项目中两者结合使用：
> - Redisson锁：防止同一商品被多个请求同时处理（应用层限流）
> - 乐观锁：防止数据库层面的并发更新（兜底）
> 
> 双层保障，更加安全。

---

### 问题7：如果支付成功但库存扣减失败怎么办？

**面试官考察点**：异常处理和事务一致性

**回答参考**：
> 通过**@Transactional**保证事务一致性：
> 
> ```java
> @Transactional
> public Result payOrder(Long orderId) {
>     // 1. 确认扣减库存
>     int rows = productMapper.confirmStock(...);
>     if (rows == 0) {
>         throw new RuntimeException("库存扣减失败");
>     }
>     
>     // 2. 更新订单状态
>     orderMapper.paySuccess(orderId);
>     
>     return Result.ok();
> }
> ```
> 
> 如果库存扣减失败，抛出异常，事务回滚，订单状态不会更新。
> 
> 同时可以引入**本地消息表**或**事务消息**，保证支付和库存扣减的最终一致性。

---

### 问题8：定时任务执行时，用户正在支付怎么办？

**面试官考察点**：并发场景处理

**回答参考**：
> 有双重保障：
> 
> 1. **支付时检查超时**：
>    ```java
>    if (order.getExpireTime().isBefore(LocalDateTime.now())) {
>        // 先释放库存，再返回失败
>        productMapper.releaseReservedStock(...);
>        orderMapper.updateStatus(orderId, 2);
>        return Result.fail("订单已超时");
>    }
>    ```
> 
> 2. **数据库乐观锁**：
>    - 如果定时任务已经释放了库存，确认扣减时会失败
>    - 不会出现库存扣减两次的情况
> 
> 两种机制配合，保证不会出错。

---

### 问题9：订单号为什么这样设计？有什么问题？

**面试官考察点**：订单号生成策略

**回答参考**：
> 当前设计：时间戳 + 6位随机数
> 
> **优点**：
> - 简单易实现
> - 可读性好（能看出下单时间）
> 
> **问题**：
> - 分布式环境下可能重复（多实例部署时）
> - 6位随机数在高并发下可能碰撞
> 
> **改进方案**：
> - 使用**雪花算法**生成ID
> - 或者加入机器ID：时间戳 + 机器ID + 序号
> - 保证分布式环境下唯一

---

### 问题10：如果Redis挂了怎么办？

**面试官考察点**：降级方案

**回答参考**：
> 有降级方案：
> 
> 1. **Redisson锁降级**：
>    - 如果Redis不可用，可以降级为数据库悲观锁（SELECT ... FOR UPDATE）
>    - 虽然性能下降，但保证功能可用
> 
> 2. **缓存降级**：
>    - 如果Redis不可用，直接查数据库
>    - 虽然性能下降，但系统不宕机
> 
> 3. **多Redis实例**：
>    - 引入Redis主从或哨兵模式
>    - 提高Redis可用性
> 
> 核心原则：缓存是锦上添花，数据库是必须的，核心流程不依赖Redis。

---

## 6. 扩展场景题目

### 场景题目1：秒杀场景优化

**题目描述**：
如果这是一个秒杀商品，1000个商品，10万人同时抢购，怎么优化？

**回答思路**：
> 1. **Redis预扣库存**：
>    - 将库存提前存入Redis
>    - 下单时直接从Redis扣减
>    - 只有Redis扣减成功才去查数据库
> 
> 2. **限流**：
>    - 网关层限流（10万请求只放1万进来）
>    - 用户级限流（同一用户最多请求10次）
> 
> 3. **异步下单**：
>    - 请求先入队列，后台异步处理
>    - 快速返回"排队中"给用户
> 
> 4. **库存预热**：
>    - 秒杀前将库存加载到Redis
> 
> 5. **答案验证**：
>    - 前端生成答案，后端验证
>    - 防机器抢购

---

### 场景题目2：多商品下单（购物车）

**题目描述**：
如果是购物车下单，一次购买多个商品，怎么保证库存一致性？

**回答思路**：
> 1. **锁升级**：
>    - 按商品ID排序，顺序加锁
>    - 避免死锁
> 
> 2. **分布式事务**：
>    - 引入Seata TCC模式
>    - Try：预扣所有商品库存
>    - Confirm：确认扣减
>    - Cancel：释放库存
> 
> 3. **本地消息表**：
>    - 记录每个商品的库存扣减状态
>    - 定时任务补偿
> 
> 4. **库存冻结**：
>    - 创建一个库存冻结表
>    - 记录每个商品冻结的库存
>    - 最终一致性

---

### 场景题目3：库存回滚问题

**题目描述**：
订单创建成功，但后续操作失败了，怎么回滚库存？

**回答思路**：
> 1. **事务回滚**：
>    - @Transactional注解
>    - 异常时自动回滚数据库
> 
> 2. **补偿机制**：
>    - 创建订单时记录操作日志
>    - 定时任务扫描失败的订单
>    - 执行补偿操作（释放库存）
> 
> 3. **本地消息表**：
>    - 记录"需要释放库存"的消息
>    - 后台任务保证消息被消费
> 
> 4. **状态机**：
>    - 订单状态流转：待支付 → 已支付/已取消
>    - 每个状态都有对应的库存处理逻辑

---

### 场景题目4：库存预警

**题目描述**：
库存低于阈值时，怎么通知商家？

**回答思路**：
> 1. **数据库触发器**：
>    - 库存变动时检查阈值
>    - 触发通知（不推荐，耦合业务）
> 
> 2. **异步消息**：
>    - 库存变动时发送消息到MQ
>    - 消费者检查阈值，发送通知
> 
> 3. **定时任务**：
>    - 定期扫描库存低于阈值的商品
>    - 发送通知给商家
> 
> 4. **Redis事件**：
>    - 库存低于阈值时设置Redis Key
>    - 监听Key过期事件发送通知

---

### 场景题目5：订单查询优化

**题目描述**：
订单表越来越大，查询变慢，怎么优化？

**回答思路**：
> 1. **分库分表**：
>    - 按user_id分库分表
>    - 同一用户的订单在同一库表
> 
> 2. **冷热分离**：
>    - 近期订单放主表
>    - 历史订单归档到历史表
> 
> 3. **索引优化**：
>    - (user_id, create_time) 复合索引
>    - (order_no) 唯一索引
> 
> 4. **缓存热点订单**：
>    - 最近30天的订单缓存
> 
> 5. **ES搜索**：
>    - 订单数据同步到ES
>    - 复杂查询走ES

---

### 场景题目6：分布式事务

**题目描述**：
订单服务和库存服务是两个独立服务，怎么保证分布式事务一致性？

**回答思路**：
> 1. **Seata AT模式**：
>    - 自动记录快照
>    - 失败时自动回滚
> 
> 2. **TCC模式**：
>    - Try：预扣库存
>    - Confirm：确认扣减
>    - Cancel：释放库存
> 
> 3. **本地消息表**：
>    - 订单服务记录消息
>    - 库存服务消费消息
>    - 保证最终一致性
> 
> 4. **事务消息**：
>    - RocketMQ事务消息
>    - 半消息机制
> 
> 推荐方案：最终一致性（本地消息表或事务消息），业务上可以接受短暂不一致。

---

### 场景题目7：防刷单

**题目描述**：
怎么防止恶意用户批量下单刷单？

**回答思路**：
> 1. **用户级别限流**：
>    - 同一用户每分钟最多下单N次
>    - 同一用户对同一商品只能有一个待支付订单
> 
> 2. **风控规则**：
>    - 新用户限制
>    - 收货地址异常检测
>    - IP/设备黑名单
> 
> 3. **验证码**：
>    - 下单时需要验证码
>    - 防止机器下单
> 
> 4. **信誉分**：
>    - 用户信誉分低于阈值时限制下单
> 
> 5. **订单审核**：
>    - 可疑订单进入审核流程
>    - 人工确认后才生效

---

### 场景题目8：库存查询性能优化

**题目描述**：
商品详情页需要显示库存，但库存查询QPS很高，怎么优化？

**回答思路**：
> 1. **多级缓存**：
>    - Caffeine本地缓存（1秒过期）
>    - Redis分布式缓存（10秒过期）
>    - 数据库兜底
> 
> 2. **库存预加载**：
>    - 热点商品库存预热到缓存
> 
> 3. **近似值显示**：
>    - 库存>100时显示"有货"
>    - 不显示精确数字
>    - 减少缓存更新频率
> 
> 4. **异步更新**：
>    - 库存变动时不立即更新缓存
>    - 后台定时任务批量刷新
> 
> 5. **读写分离**：
>    - 查询走从库
>    - 更新走主库

---

## 总结

本订单模块通过**双重锁机制**（Redisson分布式锁 + 数据库乐观锁）保证不超卖，通过**三阶段库存扣减**（预扣→确认/释放）处理订单超时，通过**删除缓存策略**保证最终一致性，通过**定时任务**自动清理超时订单，设计完善，可以应对大部分业务场景。

---

**文档版本**：v1.0  
**最后更新**：2026-05-30
