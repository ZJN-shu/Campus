# 校园论坛二手交易系统 - 面试场景题与技术改进方案

## 一、引言

本文档针对校园论坛二手交易系统，整理了面试中可能问到的实际场景题，并提供相应的技术改进方案。这些场景题涵盖了高并发、分布式系统、缓存、消息队列等核心技术领域。

---

## 二、核心业务场景题

### 场景1：秒杀场景下的库存扣减

**问题描述**：
> 在二手交易平台中，某热门商品限量100件，预计有10000人同时抢购，如何保证库存不超卖？

**当前实现**：
```java
// 当前使用乐观锁 + 分布式锁方案
// StockCacheService.java
RLock lock = redissonClient.getLock("product:lock:" + productId);
boolean isLock = lock.tryLock(1, 5, TimeUnit.SECONDS);
if (!isLock) {
    return Result.fail("系统繁忙");
}
// 乐观锁更新
productMapper.reserveStock(productId, quantity, product.getVersion());
```

**面试追问**：

| 问题 | 回答要点 |
|-----|---------|
| 为什么用乐观锁？ | 性能高，减少锁竞争 |
| 为什么用分布式锁？ | 防止多个JVM进程同时扣减 |
| 如果锁过期了怎么办？ | Redisson看门狗自动续期 |
| 库存扣减失败怎么处理？ | 返回"库存不足"或"系统繁忙" |

**技术改进方案**：

```java
// 优化后的库存扣减流程
public boolean deductStock(Long productId, Integer quantity) {
    // 1. 先尝试Redis扣减（快速失败）
    String key = "stock:" + productId;
    Long result = stringRedisTemplate.opsForValue().decrement(key);
    
    if (result == null || result < 0) {
        // 库存不足，回滚
        stringRedisTemplate.opsForValue().increment(key);
        return false;
    }
    
    // 2. 分布式锁保护数据库更新
    try (RLock lock = redissonClient.getLock("stock:lock:" + productId)) {
        if (!lock.tryLock(1, 5, TimeUnit.SECONDS)) {
            stringRedisTemplate.opsForValue().increment(key);
            return false;
        }
        
        // 3. 数据库乐观锁更新
        int updated = productMapper.decreaseStock(productId, quantity);
        if (updated == 0) {
            stringRedisTemplate.opsForValue().increment(key);
            return false;
        }
        return true;
    }
}
```

**改进点**：
1. ✅ Redis预扣减，快速失败
2. ✅ 分布式锁保护数据库
3. ✅ 异常回滚机制
4. ✅ try-with-resources自动释放锁

---

### 场景2：高并发点赞场景

**问题描述**：
> 某热门帖子在短时间内收到10万次点赞，如何保证数据正确性和系统稳定性？

**当前实现**：
```java
// LikeServiceImpl.java
// 使用Redis Stream异步处理
streamProducer.sendEvent(EventMessage.builder()
    .type("like")
    .targetType(targetType)
    .targetId(targetId)
    .userId(userId)
    .build());
```

**面试追问**：

| 问题 | 回答要点 |
|-----|---------|
| 为什么用异步？ | 提升响应速度，削峰填谷 |
| 数据一致性如何保证？ | 消息队列+批量刷库 |
| 消息丢失怎么办？ | Redis Stream ACK + 死信队列 |
| 如何防止重复点赞？ | Redis Set去重 |

**技术改进方案**：

```java
// 优化后的点赞服务
public Result like(String targetType, Long targetId) {
    Long userId = UserHolder.getUser().getId();
    
    // 1. Redis Set去重，防止重复点赞
    String likeKey = "like:" + targetType + ":" + targetId;
    Boolean isNew = stringRedisTemplate.opsForSet().add(likeKey, userId.toString());
    
    if (Boolean.FALSE.equals(isNew)) {
        // 已点赞，返回取消点赞
        stringRedisTemplate.opsForSet().remove(likeKey, userId.toString());
        stringRedisTemplate.opsForValue().decrement("like:count:" + targetType + ":" + targetId);
        return Result.ok("取消点赞成功");
    }
    
    // 2. 异步发送点赞事件
    streamProducer.sendEvent(EventMessage.builder()
        .type("like")
        .targetType(targetType)
        .targetId(targetId)
        .userId(userId)
        .build());
    
    // 3. 实时更新缓存计数
    stringRedisTemplate.opsForValue().increment("like:count:" + targetType + ":" + targetId);
    
    return Result.ok("点赞成功");
}
```

**改进点**：
1. ✅ Redis Set去重，防止重复操作
2. ✅ 实时更新缓存计数
3. ✅ 支持取消点赞
4. ✅ 异步持久化到数据库

---

### 场景3：热榜实时更新

**问题描述**：
> 热榜需要每分钟更新一次，包含10万+帖子数据，如何保证更新性能和数据一致性？

**当前实现**：
```java
// HotRankServiceImpl.java
// 使用Redis ZSet存储
redisTemplate.opsForZSet().incrementScore(HOT_RANK_KEY, member, score);
```

**面试追问**：

| 问题 | 回答要点 |
|-----|---------|
| 为什么用ZSet？ | 天然支持排序，O(logN)复杂度 |
| 如何防止热点数据？ | 时间衰减算法 |
| 数据如何同步到数据库？ | 定时任务批量同步 |
| 如果Redis挂了怎么办？ | 降级到数据库查询 |

**技术改进方案**：

```java
// 优化后的热榜更新策略
public void updateHotRank(String targetType, Long targetId, double score) {
    String key = "hot_rank:" + targetType;
    
    // 1. 时间衰减：牛顿冷却定律
    double decayFactor = Math.exp(-0.05 * getHoursSinceCreation(targetId));
    double finalScore = score * decayFactor;
    
    // 2. Redis ZSet更新
    redisTemplate.opsForZSet().incrementScore(key, targetType + ":" + targetId, finalScore);
    
    // 3. 只保留Top 1000，防止大Key
    redisTemplate.opsForZSet().removeRange(key, 0, -1001);
}

public List<Long> getHotRank(String targetType, int limit) {
    String key = "hot_rank:" + targetType;
    
    try {
        // 从Redis获取
        Set<String> topItems = redisTemplate.opsForZSet()
            .reverseRange(key, 0, limit - 1);
        
        return topItems.stream()
            .map(s -> Long.parseLong(s.split(":")[1]))
            .collect(Collectors.toList());
    } catch (Exception e) {
        // 降级到数据库
        log.warn("Redis unavailable, fallback to database");
        return hotRankMapper.selectTopHot(targetType, limit);
    }
}
```

**改进点**：
1. ✅ 时间衰减算法，新内容权重更高
2. ✅ 限制ZSet大小，防止大Key
3. ✅ Redis故障时降级到数据库
4. ✅ 分层缓存策略

---

### 场景4：订单超时处理

**问题描述**：
> 订单创建后15分钟未支付自动取消，如何高效处理大量超时订单？

**当前实现**：
```java
// OrderTimeoutScheduler.java
@Scheduled(cron = "0 * * * * ?")
public void processTimeoutOrders() {
    List<Order> timeoutOrders = orderMapper.selectTimeoutOrders();
    for (Order order : timeoutOrders) {
        productMapper.releaseReservedStock(order.getProductId(), order.getQuantity());
        orderMapper.updateStatus(order.getId(), 2);
    }
}
```

**面试追问**：

| 问题 | 回答要点 |
|-----|---------|
| 为什么用定时任务？ | 简单易实现 |
| 如果订单量很大怎么办？ | 分批处理 |
| 重复处理怎么办？ | 状态机控制 |
| 如何提升实时性？ | 延迟队列 |

**技术改进方案**：

```java
// 优化后的订单超时处理
@Scheduled(fixedDelay = 60000)
public void processTimeoutOrders() {
    int batchSize = 100;
    int offset = 0;
    
    while (true) {
        // 分页查询超时订单
        List<Order> timeoutOrders = orderMapper.selectTimeoutOrders(batchSize, offset);
        
        if (timeoutOrders.isEmpty()) {
            break;
        }
        
        // 批量处理
        for (Order order : timeoutOrders) {
            // 乐观锁更新状态，防止重复处理
            int updated = orderMapper.updateStatusWithLock(order.getId(), 0, 2);
            
            if (updated > 0) {
                // 释放库存
                productMapper.releaseReservedStock(order.getProductId(), order.getQuantity());
                // 更新缓存
                stringRedisTemplate.delete("order:" + order.getId());
            }
        }
        
        offset += batchSize;
        
        // 每批处理后短暂休眠，避免压垮数据库
        try {
            Thread.sleep(100);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            break;
        }
    }
}
```

**改进点**：
1. ✅ 分页处理，避免一次性加载过多数据
2. ✅ 乐观锁防止重复处理
3. ✅ 批量操作，减少数据库压力
4. ✅ 处理间隔休眠

**进阶方案：使用Redis延迟队列**
```java
// 订单创建时设置延迟任务
public void createOrder(Order order) {
    // 15分钟后触发
    long delay = 15 * 60 * 1000;
    String key = "order:delay";
    String value = order.getId().toString();
    
    // Redis ZSet实现延迟队列
    redisTemplate.opsForZSet().add(key, value, System.currentTimeMillis() + delay);
}

// 单独的定时任务处理延迟队列
@Scheduled(fixedDelay = 10000)
public void processDelayQueue() {
    String key = "order:delay";
    long now = System.currentTimeMillis();
    
    // 获取已到期的订单
    Set<String> expiredOrders = redisTemplate.opsForZSet()
        .rangeByScore(key, 0, now);
    
    for (String orderId : expiredOrders) {
        // 处理超时订单
        processOrderTimeout(Long.parseLong(orderId));
        // 从队列中移除
        redisTemplate.opsForZSet().remove(key, orderId);
    }
}
```

---

### 场景5：缓存一致性保证

**问题描述**：
> 商品数据同时存在于MySQL、Redis和Elasticsearch中，如何保证三者数据一致？

**当前实现**：
```java
// ProductServiceImpl.java
// 更新时删除缓存
stringRedisTemplate.delete("product:" + productId);
```

**面试追问**：

| 问题 | 回答要点 |
|-----|---------|
| 为什么删除缓存而不是更新？ | 避免缓存击穿，简化逻辑 |
| 如何处理缓存与数据库不一致？ | 最终一致性 + 定时校准 |
| ES同步延迟怎么办？ | 消息队列异步同步 |
| 缓存穿透怎么办？ | 布隆过滤器 |

**技术改进方案**：

```java
// 优化后的缓存更新策略
@Transactional
public Result updateProduct(Product product) {
    // 1. 更新数据库
    productMapper.updateById(product);
    
    // 2. 删除Redis缓存（让下次查询重新加载）
    stringRedisTemplate.delete("product:" + product.getId());
    
    // 3. 异步同步到ES
    esSyncService.syncProduct(product.getId());
    
    return Result.ok();
}

// ES同步服务
@Async
public void syncProduct(Long productId) {
    try {
        Product product = productMapper.selectById(productId);
        if (product != null) {
            ProductDocument document = convertToDocument(product);
            esService.saveProduct(document);
        }
    } catch (Exception e) {
        // 失败后加入重试队列
        retryQueue.add(productId);
    }
}

// 定时校准任务
@Scheduled(cron = "0 0 2 * * ?")
public void syncCacheWithDB() {
    List<Product> products = productMapper.selectAll();
    for (Product product : products) {
        String cacheKey = "product:" + product.getId();
        String cacheValue = stringRedisTemplate.opsForValue().get(cacheKey);
        
        if (cacheValue == null || !cacheValue.equals(product.toString())) {
            stringRedisTemplate.opsForValue().set(cacheKey, product.toString());
        }
    }
}
```

**改进点**：
1. ✅ 先更新数据库，再删除缓存
2. ✅ 异步同步ES
3. ✅ 失败重试机制
4. ✅ 定时校准任务

---

### 场景6：分布式事务处理

**问题描述**：
> 创建订单时需要同时扣减库存、创建订单记录、更新用户积分，如何保证事务一致性？

**当前实现**：
```java
// OrderServiceImpl.java
@Transactional
public Result createOrder(OrderDTO dto) {
    // 扣减库存
    productService.reserveStock(dto.getProductId(), dto.getQuantity());
    
    // 创建订单
    Order order = new Order();
    orderMapper.insert(order);
    
    // 更新用户积分
    userService.addPoints(dto.getUserId(), 100);
    
    return Result.ok(order);
}
```

**面试追问**：

| 问题 | 回答要点 |
|-----|---------|
| 当前事务有什么问题？ | 分布式环境下可能不一致 |
| 如何保证一致性？ | 分布式事务或最终一致性 |
| 使用什么方案？ | Seata AT模式或本地消息表 |
| 如何处理失败？ | 补偿机制 |

**技术改进方案**：

```java
// 使用本地消息表实现最终一致性
@Transactional
public Result createOrder(OrderDTO dto) {
    // 1. 扣减库存
    boolean success = productService.reserveStock(dto.getProductId(), dto.getQuantity());
    if (!success) {
        return Result.fail("库存不足");
    }
    
    // 2. 创建订单
    Order order = new Order();
    orderMapper.insert(order);
    
    // 3. 记录消息到本地消息表
    MessageRecord record = new MessageRecord();
    record.setOrderId(order.getId());
    record.setType("ADD_POINTS");
    record.setStatus("PENDING");
    record.setPayload("{\"userId\":" + dto.getUserId() + ",\"points\":100}");
    messageRecordMapper.insert(record);
    
    // 4. 尝试发送消息
    try {
        messageProducer.send(record);
        record.setStatus("SENT");
        messageRecordMapper.updateById(record);
    } catch (Exception e) {
        // 发送失败，状态保持PENDING，等待重试
        log.error("Send message failed: {}", e.getMessage());
    }
    
    return Result.ok(order);
}

// 消息重试任务
@Scheduled(fixedDelay = 60000)
public void retryFailedMessages() {
    List<MessageRecord> pendingRecords = messageRecordMapper.selectPending();
    
    for (MessageRecord record : pendingRecords) {
        try {
            messageProducer.send(record);
            record.setStatus("SENT");
            messageRecordMapper.updateById(record);
        } catch (Exception e) {
            record.setRetryCount(record.getRetryCount() + 1);
            messageRecordMapper.updateById(record);
            
            if (record.getRetryCount() >= 3) {
                record.setStatus("FAILED");
                messageRecordMapper.updateById(record);
                // 人工介入处理
                alertService.alert("消息发送失败: " + record.getId());
            }
        }
    }
}
```

**改进点**：
1. ✅ 本地消息表记录
2. ✅ 消息重试机制
3. ✅ 失败告警
4. ✅ 最终一致性保证

---

### 场景7：系统限流与降级

**问题描述**：
> 系统遭遇突发流量（如秒杀活动），如何保证系统稳定性？

**当前实现**：
```java
// RateLimitInterceptor.java
@RateLimit(value = 100, timeout = 1)
public Result createPost(Post post) { ... }
```

**面试追问**：

| 问题 | 回答要点 |
|-----|---------|
| 使用什么限流算法？ | 令牌桶算法 |
| 如何实现分布式限流？ | Redis + Lua脚本 |
| 限流后的处理策略？ | 返回友好提示或降级处理 |
| 如何动态调整限流阈值？ | 配置中心动态配置 |

**技术改进方案**：

```java
// 分布式限流实现
public boolean tryAcquire(String key, int limit, int timeout) {
    String redisKey = "rate:limit:" + key;
    String script = """
        local current = redis.call('get', KEYS[1])
        if current and tonumber(current) >= tonumber(ARGV[1]) then
            return 0
        end
        current = redis.call('incr', KEYS[1])
        if tonumber(current) == 1 then
            redis.call('expire', KEYS[1], ARGV[2])
        end
        return 1
    """;
    
    Long result = redisTemplate.execute(
        new DefaultRedisScript<>(script, Long.class),
        Collections.singletonList(redisKey),
        limit, timeout
    );
    
    return result != null && result == 1;
}

// 限流拦截器
public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
    String key = request.getRequestURI();
    
    // 获取动态配置的限流阈值
    int limit = configService.getLimit(key);
    
    if (!rateLimitService.tryAcquire(key, limit, 60)) {
        response.setStatus(HttpStatus.TOO_MANY_REQUESTS.value());
        response.getWriter().write(JSON.toJSONString(Result.fail("请求过于频繁，请稍后再试")));
        return false;
    }
    
    return true;
}
```

**改进点**：
1. ✅ Redis + Lua脚本实现分布式限流
2. ✅ 动态配置限流阈值
3. ✅ 友好的限流提示
4. ✅ 精确到接口级别的限流

---

### 场景8：全链路追踪

**问题描述**：
> 用户反馈某个请求响应慢，如何快速定位问题？

**面试追问**：

| 问题 | 回答要点 |
|-----|---------|
| 使用什么工具？ | SkyWalking或Sleuth |
| 需要追踪哪些指标？ | 调用链、延迟、错误率 |
| 如何与日志关联？ | TraceId贯穿全链路 |
| 如何告警？ | 根据延迟和错误率配置告警规则 |

**技术改进方案**：

```java
// 自定义请求拦截器，添加TraceId
public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
    String traceId = UUID.randomUUID().toString();
    request.setAttribute("traceId", traceId);
    MDC.put("traceId", traceId);
    return true;
}

public void afterCompletion(HttpServletRequest request, HttpServletResponse response, 
                            Object handler, Exception ex) {
    MDC.remove("traceId");
}

// 日志格式配置
// logback.xml
<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%X{traceId}] - %msg%n</pattern>

// 使用SkyWalking进行全链路追踪
// pom.xml添加依赖
<dependency>
    <groupId>org.apache.skywalking</groupId>
    <artifactId>apm-toolkit-trace</artifactId>
    <version>9.4.0</version>
</dependency>

// 在关键方法上添加追踪注解
@Trace(operationName = "createOrder")
public Result createOrder(OrderDTO dto) {
    // ...
}
```

**改进点**：
1. ✅ TraceId贯穿全链路
2. ✅ 日志关联TraceId
3. ✅ SkyWalking全链路追踪
4. ✅ 关键方法追踪

---

## 三、技术架构改进建议

### 3.1 当前架构

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端                              │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                     API Gateway                            │
│  (限流、熔断、日志、监控)                                   │
└──────────────────────────┬──────────────────────────────────┘
                           │
            ┌──────────────┼──────────────┐
            ▼              ▼              ▼
    ┌───────────┐    ┌───────────┐    ┌───────────┐
    │   订单    │    │   商品    │    │   用户    │
    │  Service  │    │  Service  │    │  Service  │
    └─────┬─────┘    └─────┬─────┘    └─────┬─────┘
          │                 │                 │
          └────────┬────────┴────────┬────────┘
                   ▼                 ▼
           ┌───────────┐      ┌───────────┐
           │   MySQL   │      │   Redis   │
           │  (主从)   │      │ (Cluster) │
           └───────────┘      └───────────┘
```

### 3.2 改进后架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        客户端                                         │
└──────────────────────────┬──────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     API Gateway (Nginx + Spring Cloud Gateway)         │
│  (限流、熔断、日志、监控、灰度发布)                                     │
└──────────────────────────┬──────────────────────────────────────────────┘
                           │
            ┌──────────────┼──────────────┬──────────────┐
            ▼              ▼              ▼              ▼
    ┌───────────┐    ┌───────────┐    ┌───────────┐    ┌───────────┐
    │   订单    │    │   商品    │    │   用户    │    │   搜索    │
    │  Service  │    │  Service  │    │  Service  │    │  Service  │
    │   (多实例) │    │   (多实例) │    │   (多实例) │    │   (ES)   │
    └─────┬─────┘    └─────┬─────┘    └─────┬─────┘    └───────────┘
          │                 │                 │
          └────────┬────────┴────────┬────────┘
                   ▼                 ▼
           ┌───────────┐      ┌───────────┐
           │   MySQL   │      │   Redis   │
           │  (读写分离)│      │ (Sentinel)│
           └───────────┘      └───────────┘
                   │
                   ▼
           ┌───────────┐
           │  Message  │
           │   Queue   │
           │ (RocketMQ)│
           └───────────┘
```

### 3.3 改进清单

| 改进项 | 当前状态 | 目标状态 | 优先级 |
|-------|---------|---------|--------|
| Redis集群 | 单点 | Sentinel/Cluster | 🔴 高 |
| 数据库读写分离 | 单库 | 主从复制 | 🟡 中 |
| 消息队列 | Redis Stream | RocketMQ | 🟡 中 |
| 服务拆分 | 单体 | 微服务 | 🟡 中 |
| 服务发现 | 无 | Nacos | 🟡 中 |
| 配置中心 | 无 | Nacos | 🟡 中 |
| 全链路追踪 | 无 | SkyWalking | 🟢 低 |
| 监控告警 | 基础 | Prometheus + Grafana | 🟢 低 |
| 容器化部署 | 无 | Docker + K8s | 🟢 低 |

---

## 四、面试场景题汇总

### 4.1 高频场景题

| 序号 | 场景题 | 考察要点 |
|-----|-------|---------|
| 1 | 秒杀库存扣减 | 分布式锁、乐观锁、Redis预扣减 |
| 2 | 高并发点赞 | 异步处理、消息队列、数据去重 |
| 3 | 热榜实时更新 | Redis ZSet、时间衰减、大Key优化 |
| 4 | 订单超时处理 | 定时任务、延迟队列、状态机 |
| 5 | 缓存一致性 | 缓存策略、最终一致性、定时校准 |
| 6 | 分布式事务 | 本地消息表、Seata、补偿机制 |
| 7 | 系统限流降级 | 令牌桶、分布式限流、熔断降级 |
| 8 | 全链路追踪 | TraceId、SkyWalking、日志关联 |

### 4.2 回答技巧

1. **先分析问题**：理解场景的核心痛点
2. **说明当前方案**：介绍项目中的实现方式
3. **指出改进空间**：分析现有方案的不足
4. **提供优化方案**：给出具体的改进代码或架构
5. **考虑边界情况**：异常处理、失败重试、降级策略

---

## 五、总结

本文档整理了校园论坛二手交易系统的8个核心业务场景题，涵盖了：
- ✅ 高并发处理
- ✅ 分布式事务
- ✅ 缓存策略
- ✅ 消息队列
- ✅ 系统稳定性

每个场景都提供了：
1. 问题描述
2. 当前实现
3. 面试追问
4. 技术改进方案

这些内容可以帮助您在面试中更好地展示对项目的理解和技术深度。

---

**文档版本**：v1.0  
**生成时间**：2026-05-23
