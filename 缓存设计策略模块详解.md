# 缓存设计策略模块详解

## 目录

1. [模块概述](#一模块概述)
2. [架构设计](#二架构设计)
3. [核心实现](#三核心实现)
4. [缓存更新策略](#四缓存更新策略)
5. [面试常见问题](#五面试常见问题)
6. [优化建议](#六优化建议)

---

## 一、模块概述

### 1.1 设计目标
本项目采用**多级缓存架构**（本地缓存 + Redis分布式缓存），旨在：
- **提高性能**：将热点数据缓存，减少数据库压力
- **降低延迟**：本地缓存（L1）访问延迟在微秒级
- **保证一致性**：通过合理的缓存失效策略保证数据最终一致
- **高可用性**：多级缓存互为备份，Redis故障时可降级到数据库

### 1.2 核心组件
| 组件 | 技术选型 | 作用 |
|------|----------|------|
| L1缓存 | Caffeine | 本地缓存，高性能，内存存储 |
| L2缓存 | Redis | 分布式缓存，多实例共享 |
| 数据加载器 | Function | 缓存未命中时从数据库加载 |
| 统计监控 | Caffeine Stats | 记录缓存命中率等指标 |

---

## 二、架构设计

### 2.1 多级缓存架构图

```
用户请求
    ↓
┌─────────────────────────────────────┐
│  L1本地缓存（Caffeine）               │
│  - 最大10000条                        │
│  - 写入后5分钟过期                    │
│  - 微秒级访问                         │
└─────────────────────────────────────┘
    ↓ 未命中
┌─────────────────────────────────────┐
│  L2 Redis缓存                        │
│  - 可配置TTL（默认30分钟）            │
│  - 毫秒级访问                         │
│  - 多实例共享                         │
└─────────────────────────────────────┘
    ↓ 未命中
┌─────────────────────────────────────┐
│  MySQL数据库                          │
│  - 兜底查询                           │
│  - 数据加载后回填L1和L2               │
└─────────────────────────────────────┘
```

### 2.2 数据流向设计

```
查询流程：
1. 查询L1本地缓存 → 命中直接返回
2. 未命中，查询L2 Redis → 命中则回填L1并返回
3. 未命中，查询数据库 → 加载数据后回填L1和L2

更新流程：
1. 更新数据库
2. 删除L1和L2缓存（而非更新）
3. 下次查询时重新加载最新数据
```

---

## 三、核心实现

### 3.1 MultiLevelCache 多级缓存核心类

**文件位置**：[MultiLevelCache.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\cache\MultiLevelCache.java)

#### 核心代码：

```java
@Component
public class MultiLevelCache {
    
    @Resource
    private StringRedisTemplate stringRedisTemplate;
    
    /**
     * 本地缓存（Caffeine）配置
     */
    private final Cache<String, String> localCache = Caffeine.newBuilder()
            .maximumSize(10000)           // 最大容量10000条
            .expireAfterWrite(5, TimeUnit.MINUTES)  // 写入后5分钟过期
            .recordStats()                // 记录命中统计
            .build();
    
    /**
     * 获取缓存（完整流程）
     */
    public String get(String key, Function<String, String> loader, long ttl) {
        // 1. 查L1本地缓存
        String value = localCache.getIfPresent(key);
        if (value != null) {
            log.debug("本地缓存命中: {}", key);
            return value;
        }
        
        // 2. 查L2 Redis
        value = stringRedisTemplate.opsForValue().get(key);
        if (value != null) {
            log.debug("Redis缓存命中: {}", key);
            localCache.put(key, value);  // 回填L1
            return value;
        }
        
        // 3. 查数据库（通过loader）
        value = loader.apply(key);
        
        // 4. 回填缓存
        if (value != null) {
            stringRedisTemplate.opsForValue().set(key, value, ttl, TimeUnit.MINUTES);
            localCache.put(key, value);
        }
        
        return value;
    }
    
    /**
     * 删除缓存（同时删除L1和L2）
     */
    public void evict(String key) {
        localCache.invalidate(key);
        stringRedisTemplate.delete(key);
    }
}
```

### 3.2 StockCacheService 库存缓存服务

**文件位置**：[StockCacheService.java](file:///d:\Code\campus%20-%201\src\main\java\com\campus\service\Impl\StockCacheService.java)

#### 核心代码：

```java
@Service
public class StockCacheService {
    
    @Resource
    private ProductMapper productMapper;
    
    @Resource
    private StringRedisTemplate stringRedisTemplate;
    
    @Resource
    private MultiLevelCache multiLevelCache;
    
    private static final String CACHE_STOCK_KEY = "cache:stock:";
    
    /**
     * 获取库存（特殊处理：不使用多级缓存，直接查Redis）
     * 原因：库存数据敏感，一致性要求高，TTL设置较短（10秒）
     */
    public Integer getAvailableStock(Long productId) {
        String cacheKey = CACHE_STOCK_KEY + productId;
        
        // 1. 查Redis
        Boolean hasKey = stringRedisTemplate.hasKey(cacheKey);
        if (Boolean.TRUE.equals(hasKey)) {
            String stockStr = stringRedisTemplate.opsForValue().get(cacheKey);
            if (stockStr != null) {
                return Integer.parseInt(stockStr);
            }
        }
        
        // 2. 查数据库
        Product product = productMapper.selectById(productId);
        if (product == null) {
            return 0;
        }
        
        int stock = product.getStock() != null ? product.getStock() : 0;
        int reservedStock = product.getReservedStock() != null ? product.getReservedStock() : 0;
        int availableStock = stock - reservedStock;
        
        // 3. 写入缓存（10秒过期）
        stringRedisTemplate.opsForValue().set(cacheKey, String.valueOf(availableStock), 10, TimeUnit.SECONDS);
        
        return availableStock;
    }
    
    /**
     * 库存变动时：删除缓存
     */
    public void onReserveStock(Long productId, Integer quantity) {
        String stockKey = CACHE_STOCK_KEY + productId;
        stringRedisTemplate.delete(stockKey);  // 删除而非更新
        multiLevelCache.evict(CACHE_PRODUCT_KEY + productId);
    }
}
```

### 3.3 Caffeine 配置说明

```java
Caffeine.newBuilder()
    .maximumSize(10000)           // 最大容量，超出时按LRU淘汰
    .expireAfterWrite(5, TimeUnit.MINUTES)  // 写入后过期策略
    .recordStats()                // 启用统计
    .build();
```

| 配置项 | 说明 |
|--------|------|
| maximumSize | 本地缓存最大条数，超出时按LRU策略淘汰老数据 |
| expireAfterWrite | 写入后固定时间过期（访问不会刷新） |
| recordStats | 记录命中率、命中次数等统计信息 |

---

## 四、缓存更新策略

### 4.1 缓存更新策略对比

| 策略 | 说明 | 优点 | 缺点 | 适用场景 |
|------|------|------|------|----------|
| Cache Aside（本项目采用） | 更新数据库 → 删除缓存 | 简单易实现，一致性较好 | 删除瞬间可能有不一致 | 大多数场景 |
| Write Through | 先更新缓存 → 再更新数据库 | 一致性较好 | 复杂度高，写入慢 | 对一致性要求极高 |
| Write Behind | 先更新缓存 → 异步更新数据库 | 写入速度快 | 一致性风险高 | 日志、统计等允许丢失的场景 |
| Refresh Ahead | 后台定时刷新缓存 | 命中率高 | 资源消耗大 | 热点数据预测准确的场景 |

### 4.2 本项目策略详解

本项目采用**Cache Aside + Delete Cache**策略：

```
数据更新流程：
┌─────────────────────────────────────────┐
│ 1. 更新数据库                            │
│    update product set ... where ...      │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│ 2. 删除缓存（L1和L2）                     │
│    stringRedisTemplate.delete(key)       │
│    localCache.invalidate(key)            │
└─────────────────────────────────────────┘

下次查询流程：
┌─────────────────────────────────────────┐
│ 1. 查询缓存 → 未命中                      │
│ 2. 查询数据库 → 加载最新数据              │
│ 3. 回填缓存                              │
└─────────────────────────────────────────┘
```

### 4.3 为什么采用删除而不是更新？

**问题场景**：
```
时间线：
T1 - 线程A更新数据库：value = 10
T2 - 线程B更新数据库：value = 20
T3 - 线程B更新缓存：cache = 20
T4 - 线程A更新缓存：cache = 10

结果：缓存是10，数据库是20 → 数据不一致！
```

**解决方案**：删除缓存，而不是更新缓存
```
时间线：
T1 - 线程A更新数据库：value = 10
T2 - 线程B更新数据库：value = 20
T3 - 线程B删除缓存
T4 - 线程A删除缓存
T5 - 线程C查询缓存 → 未命中 → 查数据库 → value = 20 → 回填缓存

结果：缓存和数据库一致！
```

---

## 五、面试常见问题

### 问题1：为什么需要多级缓存？只用Redis不行吗？

**面试官考察点**：对本地缓存和分布式缓存优缺点的理解

**回答参考**：
> 用多级缓存主要是为了**性能和可靠性**：
>
> 1. **性能方面**：
>    - 本地缓存（Caffeine）访问延迟在微秒级
>    - Redis访问延迟在毫秒级
>    - 热点数据命中L1，性能提升100倍以上
>
> 2. **可靠性方面**：
>    - L1和L2互为备份，Redis故障时可降级到数据库
>    - 减少Redis网络IO，降低Redis压力
>
> 3. **成本方面**：
>    - 本地缓存不占用网络带宽
>    - 降低Redis成本
>
> 当然，多级缓存也增加了一致性复杂度，但通过合理的失效策略可以解决。

---

### 问题2：缓存和数据库的一致性怎么保证？

**面试官考察点**：对缓存一致性策略的理解

**回答参考**：
> 我们采用**Cache Aside + 删除缓存**策略，保证最终一致性：
>
> 1. **更新流程**：先更新数据库，再删除缓存
> 2. **查询流程**：缓存未命中时查数据库，然后回填缓存
> 3. **最终一致性**：通过TTL保证即使有不一致，也会在过期后自动修正
>
> 为什么是先更新数据库，再删除缓存？
> - 如果先删缓存，在更新数据库前有查询进来，会把旧数据回填
> - 先更新数据库，再删缓存，即使删缓存失败，TTL也会兜底
>
> 一致性方案对比：
> - 强一致性：分布式锁（性能差）
> - 最终一致性：Cache Aside（本项目采用，平衡性能和一致性）
> - 弱一致性：定时刷新（一致性最差）

---

### 问题3：缓存击穿、缓存穿透、缓存雪崩分别是什么？怎么解决？

**面试官考察点**：经典缓存问题及其解决方案

**回答参考**：
> **缓存击穿**：热点key失效，大量请求打到数据库
> - 解决方案：互斥锁（分布式锁）、热点数据永不过期
>
> **缓存穿透**：查询不存在的数据，缓存和数据库都没有
> - 解决方案：布隆过滤器拦截、缓存空值
>
> **缓存雪崩**：大量key同时失效，数据库压力暴增
> - 解决方案：TTL加随机值、多级缓存、降级限流
>
> 本项目方案：
> - 通过多级缓存降低雪崩影响
> - 库存缓存TTL较短（10秒），避免长时间不一致
> - 有兴趣可以引入布隆过滤器防止穿透

---

### 问题4：本地缓存和Redis缓存的数据不一致怎么办？

**面试官考察点**：分布式环境下的一致性问题

**回答参考**：
> 本地缓存确实会导致多实例数据不一致：
>
> 问题场景：
> - 用户1请求实例A，缓存数据为V1
> - 用户2更新数据为V2，实例A删除本地缓存
> - 用户3请求实例B，缓存数据还是V1
>
> 解决方案：
> 1. **TTL兜底**：本项目L1缓存5分钟过期，最多不一致5分钟
> 2. **消息队列广播失效**：Redis Pub/Sub或MQ广播缓存失效事件
> 3. **接受最终一致性**：大多数业务场景可以接受短时间不一致
>
> 本项目选择了方案1 + 方案3，在一致性和复杂度之间平衡。

---

### 问题5：为什么用Caffeine而不是Guava Cache？

**面试官考察点**：缓存技术选型对比

**回答参考**：
> Caffeine是Guava Cache的改进版，主要优势：
>
> 1. **性能更好**：
>    - 采用Window TinyLFU淘汰算法，比LRU命中率更高
>    - 高并发下性能是Guava Cache的2-5倍
>
> 2. **功能更丰富**：
>    - 支持异步刷新
>    - 更灵活的过期策略
>
> 3. **Spring推荐**：
>    - Spring Boot 2.0+ 默认推荐Caffeine
>    - Spring Cache抽象支持Caffeine
>
> 本项目配置：
> - maximumSize: 10000条
> - expireAfterWrite: 5分钟过期
> - recordStats: 记录命中率

---

### 问题6：缓存容量满了怎么办？淘汰策略是什么？

**面试官考察点**：缓存淘汰算法的理解

**回答参考**：
> 本项目L1缓存采用Caffeine，默认淘汰策略是**Window TinyLFU**：
>
> 传统算法：
> - LRU（最近最少使用）：简单但对突发流量不友好
> - LFU（最不经常使用）：需要维护频率，内存开销大
>
> TinyLFU优势：
> - 结合LRU和LFU优点
> - 使用频率统计，命中率更高
> - 内存占用小
>
> 本项目配置maximumSize=10000条，超出时按Window TinyLFU淘汰。

---

### 问题7：怎么监控缓存效果？

**面试官考察点**：缓存可观测性

**回答参考**：
> 我们通过以下方式监控：
>
> 1. **命中率统计**：
>    ```java
>    // Caffeine内置统计
>    public double getLocalHitRate() {
>        return localCache.stats().hitRate();
>    }
>    ```
>
> 2. **日志监控**：
>    - 记录缓存命中、未命中日志
>    - 定期输出统计信息
>
> 3. **监控指标**：
>    - 命中率目标：85%+
>    - 低于80%时报警
>
> 4. **Redis监控**：
>    - Redis内存使用率
>    - Key数量
>    - 慢查询日志

---

### 问题8：Redis挂了怎么办？

**面试官考察点**：降级方案设计

**回答参考**：
> Redis故障时有降级方案：
>
> 1. **查询降级**：
>    - Redis不可用时，直接查数据库
>    - 虽然性能下降，但系统不宕机
>
> 2. **本地缓存兜底**：
>    - 即使Redis挂了，L1缓存还能工作
>    - 减少部分数据库压力
>
> 3. **高可用部署**：
>    - Redis主从+哨兵模式
>    - 或Redis Cluster集群
>
> 核心原则：缓存是锦上添花，数据库是必须的，核心流程不依赖缓存。

---

### 问题9：库存缓存为什么不使用多级缓存？

**面试官考察点**：不同场景下的缓存策略选择

**回答参考**：
> 库存是**敏感数据**，一致性要求高，所以特殊处理：
>
> 1. **不使用L1本地缓存**：
>    - 避免多实例数据不一致
>    - 库存经常变化，本地缓存没意义
>
> 2. **TTL设置较短（10秒）**：
>    - 即使有不一致，最多10秒
>    - 平衡性能和一致性
>
> 3. **更新即删除**：
>    - 库存变动时立即删除缓存
>    - 下次查询重新加载
>
> 不同数据不同策略：
> - 热点读多写少：用多级缓存（如商品详情）
> - 一致性要求高：直接用Redis+短TTL（如库存）
> - 频繁变动：考虑不用缓存

---

### 问题10：怎么设计缓存Key？

**面试官考察点**：缓存Key设计规范

**回答参考**：
> 本项目Key设计规范：
>
> ```
> 格式：业务前缀:数据类型:业务ID
> 示例：
> - cache:stock:1001        // 商品1001的库存
> - cache:product:1001      // 商品1001的详情
> - cache:post:2001         // 帖子2001的内容
> ```
>
> 设计原则：
> 1. **可读性**：能看出是什么数据
> 2. **唯一性**：同一数据Key唯一
> 3. **前缀管理**：按业务分组，方便批量管理
> 4. **控制长度**：避免Key过长（Redis Key限制512MB）
>
> 最佳实践：
> - 使用冒号分隔
> - 统一使用小写
> - 不包含特殊字符

---

## 六、优化建议

### 6.1 短期优化

1. **引入布隆过滤器**：防止缓存穿透
   ```java
   // 先查布隆过滤器，存在才查缓存/数据库
   if (bloomFilter.mightContain(key)) {
       // 查询缓存
   }
   ```

2. **缓存预热**：系统启动时加载热点数据
   ```java
   @PostConstruct
   public void preloadHotData() {
       // 加载热门商品
       List<Long> hotProductIds = productMapper.getHotProductIds();
       for (Long id : hotProductIds) {
           multiLevelCache.get("cache:product:" + id, k -> loadFromDB(k));
       }
   }
   ```

3. **监控告警**：配置Prometheus + Grafana
   - 缓存命中率告警（<80%）
   - Redis内存使用率告警（>80%）

### 6.2 长期优化

1. **缓存失效广播**：使用Redis Pub/Sub广播失效事件
   ```java
   // 发布失效事件
   stringRedisTemplate.convertAndSend("cache:invalidation", key);
   
   // 订阅失效事件
   @RedisListener(channel = "cache:invalidation")
   public void handleInvalidation(String key) {
       localCache.invalidate(key);
   }
   ```

2. **二级缓存支持**：本地缓存 + Redis集群
   - 引入Redis Cluster提高容量和可用性

3. **热点Key自动发现**：
   - 统计访问频率
   - 自动调整热点数据的TTL

---

## 总结

本项目通过**多级缓存架构**（Caffeine本地缓存 + Redis分布式缓存）实现高性能缓存方案，采用**Cache Aside + 删除缓存**策略保证最终一致性，通过合理的TTL设计和监控体系保证缓存质量。

---

**文档版本**：v1.0  
**最后更新**：2026-05-30
