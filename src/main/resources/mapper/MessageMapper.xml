<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.mapper.MessageMapper">

    <resultMap id="MessageWithUserMap" type="com.campus.entity.Message">
        <id property="id" column="id"/>
        <result property="fromUserId" column="from_user_id"/>
        <result property="toUserId" column="to_user_id"/>
        <result property="content" column="content"/>
        <result property="type" column="type"/>
        <result property="targetType" column="target_type"/>
        <result property="targetId" column="target_id"/>
        <result property="isRead" column="is_read"/>
        <result property="createTime" column="create_time"/>
        <result property="fromUserName" column="from_user_name"/>
        <result property="fromUserAvatar" column="from_user_avatar"/>
    </resultMap>

    <select id="selectMessagesWithUser" resultMap="MessageWithUserMap">
        SELECT
        m.*,
        s.nick_name as from_user_name,
        s.avatar as from_user_avatar
        FROM tb_message m
        LEFT JOIN tb_student s ON m.from_user_id = s.id
        WHERE m.to_user_id = #{userId}
        <if test="type != null and type != 0">
            AND m.type = #{type}
        </if>
        ORDER BY m.create_time DESC
        LIMIT #{offset}, #{limit}
    </select>
</mapper>